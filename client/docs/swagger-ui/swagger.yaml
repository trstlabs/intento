swagger: '2.0'
info:
  title: Intento
  description: A REST interface for queries and transactions
  version: ''
paths:
  /intento/alloc/v1/params:
    get:
      operationId: AllocParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  distribution_proportions:
                    title: >-
                      distribution_proportions defines the proportion of the
                      minted denom
                    type: object
                    properties:
                      developer_rewards:
                        type: string
                      community_pool:
                        type: string
                      relayer_incentives:
                        type: string
                    description: >-
                      DistributionProportions defines the proportion that each
                      bucket  receives.
                  weighted_developer_rewards_receivers:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                        weight:
                          type: string
                      description: WeightedAddress defines an address with a weight.
                    title: addresses to receive developer rewards
                  supplement_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      SupplementAmount is the amount to be supplemented from the
                      pool on top of

                      newly minted coins.
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Query
  /intento/claim/v1/claim_record/{address}:
    get:
      operationId: ClaimRecord
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              claim_record:
                type: object
                properties:
                  address:
                    type: string
                    title: address of recipient
                  maximum_claimable_amount:
                    title: maximum claimable amount for the address
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  status:
                    type: array
                    items:
                      type: object
                      properties:
                        action_completed:
                          type: boolean
                          title: true if action is completed
                        vesting_periods_completed:
                          type: array
                          items:
                            type: boolean
                          title: >-
                            true if action is completed

                            index refers to the 4 vesting periods for the given
                            action
                        vesting_periods_claimed:
                          type: array
                          items:
                            type: boolean
                          title: >-
                            true if action is completed

                            index refers to the 4 vesting periods for the given
                            action
                      title: >-
                        Status contains for an action if it is completed and
                        claimed
                    title: 'index of status array refers to action enum #'
                title: A Claim Records is the metadata of claim data per address
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
      tags:
        - Query
  /intento/claim/v1/claimable_for_action/{address}/{action}:
    get:
      operationId: ClaimableForAction
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              total:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
        - name: action
          in: path
          required: true
          type: string
          enum:
            - ACTION_ACTION_LOCAL
            - ACTION_ACTION_ICA
            - ACTION_GOVERNANCE_VOTE
            - ACTION_DELEGATE_STAKE
      tags:
        - Query
  /intento/claim/v1/module_account_balance:
    get:
      operationId: ModuleAccountBalance
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              moduleAccountBalance:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: params defines the parameters of the module.
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Query
  /intento/claim/v1/params:
    get:
      operationId: ClaimParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  airdrop_start_time:
                    type: string
                    format: date-time
                  duration_until_decay:
                    type: string
                  duration_of_decay:
                    type: string
                  claim_denom:
                    type: string
                    title: denom of claimable asset
                  duration_vesting_periods:
                    type: array
                    items:
                      type: string
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Query
  /intento/claim/v1/total_claimable/{address}:
    get:
      operationId: TotalClaimable
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              total:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
      tags:
        - Query
  /intento/intent/v1/address-to-ica:
    get:
      summary: >-
        QueryInterchainAccountFromAddress returns the interchain account for
        given

        owner address on a given connection pair
      operationId: InterchainAccountFromAddress
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              interchain_account_address:
                type: string
            title: >-
              QueryInterchainAccountFromAddressResponse the response type for
              the

              Query/InterchainAccountAddress RPC
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: owner
          in: query
          required: false
          type: string
        - name: connection_id
          in: query
          required: false
          type: string
      tags:
        - Query
  /intento/intent/v1/flow-history/{id}:
    get:
      summary: FlowHistory returns the flow history
      operationId: FlowHistory
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              history:
                type: array
                items:
                  type: object
                  properties:
                    scheduled_exec_time:
                      type: string
                      format: date-time
                    actual_exec_time:
                      type: string
                      format: date-time
                    exec_fee:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                    executed:
                      type: boolean
                      title: >-
                        whether all messages are executed, independent of
                        succesfull result
                    timed_out:
                      type: boolean
                      title: timed out from execution over IBC
                    errors:
                      type: array
                      items:
                        type: string
                      title: >-
                        errors from execution, if executed and no error the
                        execution was

                        succesfull
                    msg_responses:
                      type: array
                      items:
                        type: object
                        properties:
                          '@type':
                            type: string
                            description: >-
                              A URL/resource name that uniquely identifies the
                              type of the serialized

                              protocol buffer message. This string must contain
                              at least

                              one "/" character. The last segment of the URL's
                              path must represent

                              the fully qualified name of the type (as in

                              `path/google.protobuf.Duration`). The name should
                              be in a canonical form

                              (e.g., leading "." is not accepted).


                              In practice, teams usually precompile into the
                              binary all types that they

                              expect it to use in the context of Any. However,
                              for URLs which use the

                              scheme `http`, `https`, or no scheme, one can
                              optionally set up a type

                              server that maps type URLs to message definitions
                              as follows:


                              * If no scheme is provided, `https` is assumed.

                              * An HTTP GET on the URL must yield a
                              [google.protobuf.Type][]
                                value in binary format, or produce an error.
                              * Applications are allowed to cache lookup results
                              based on the
                                URL, or have them precompiled into a binary to avoid any
                                lookup. Therefore, binary compatibility needs to be preserved
                                on changes to types. (Use versioned type names to manage
                                breaking changes.)

                              Note: this functionality is not currently
                              available in the official

                              protobuf release, and it is not used for type URLs
                              beginning with

                              type.googleapis.com. As of May 2023, there are no
                              widely used type server

                              implementations and no plans to implement one.


                              Schemes other than `http`, `https` (or the empty
                              scheme) might be

                              used with implementation specific semantics.
                        additionalProperties: {}
                        description: >-
                          `Any` contains an arbitrary serialized protocol buffer
                          message along with a

                          URL that describes the type of the serialized message.


                          Protobuf library provides support to pack/unpack Any
                          values in the form

                          of utility functions or additional generated methods
                          of the Any type.


                          Example 1: Pack and unpack a message in C++.

                              Foo foo = ...;
                              Any any;
                              any.PackFrom(foo);
                              ...
                              if (any.UnpackTo(&foo)) {
                                ...
                              }

                          Example 2: Pack and unpack a message in Java.

                              Foo foo = ...;
                              Any any = Any.pack(foo);
                              ...
                              if (any.is(Foo.class)) {
                                foo = any.unpack(Foo.class);
                              }
                              // or ...
                              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                                foo = any.unpack(Foo.getDefaultInstance());
                              }

                           Example 3: Pack and unpack a message in Python.

                              foo = Foo(...)
                              any = Any()
                              any.Pack(foo)
                              ...
                              if any.Is(Foo.DESCRIPTOR):
                                any.Unpack(foo)
                                ...

                           Example 4: Pack and unpack a message in Go

                               foo := &pb.Foo{...}
                               any, err := anypb.New(foo)
                               if err != nil {
                                 ...
                               }
                               ...
                               foo := &pb.Foo{}
                               if err := any.UnmarshalTo(foo); err != nil {
                                 ...
                               }

                          The pack methods provided by protobuf library will by
                          default use

                          'type.googleapis.com/full.type.name' as the type URL
                          and the unpack

                          methods only use the fully qualified type name after
                          the last '/'

                          in the type URL, for example "foo.bar.com/x/y.z" will
                          yield type

                          name "y.z".


                          JSON

                          ====

                          The JSON representation of an `Any` value uses the
                          regular

                          representation of the deserialized, embedded message,
                          with an

                          additional field `@type` which contains the type URL.
                          Example:

                              package google.profile;
                              message Person {
                                string first_name = 1;
                                string last_name = 2;
                              }

                              {
                                "@type": "type.googleapis.com/google.profile.Person",
                                "firstName": <string>,
                                "lastName": <string>
                              }

                          If the embedded message type is well-known and has a
                          custom JSON

                          representation, that representation will be embedded
                          adding a field

                          `value` which holds the custom JSON in addition to the
                          `@type`

                          field. Example (for message
                          [google.protobuf.Duration][]):

                              {
                                "@type": "type.googleapis.com/google.protobuf.Duration",
                                "value": "1.212s"
                              }
                      title: will be empty when save_responses is false
                    query_responses:
                      type: array
                      items:
                        type: string
                      title: will be empty when save_responses is false
                    packet_sequences:
                      type: array
                      items:
                        type: string
                        format: uint64
                      title: packet sequences of the flow execution on the host chain
                  title: >-
                    FlowHistoryEntry provides a the history of flow interchain
                    tx call
              pagination:
                description: Pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: |-
              QueryFlowHistoryResponse the response type for the
              Query/QueryFlowHistoryResponse RPC
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: id of the flow
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /intento/intent/v1/flow/{id}:
    get:
      summary: Flow returns the auto-executing interchain account tx
      operationId: Flow
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              flow:
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                  owner:
                    type: string
                  label:
                    type: string
                  fee_address:
                    type: string
                  msgs:
                    type: array
                    items:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                  interval:
                    type: string
                  start_time:
                    type: string
                    format: date-time
                  exec_time:
                    type: string
                    format: date-time
                  end_time:
                    type: string
                    format: date-time
                  update_history:
                    type: array
                    items:
                      type: string
                      format: date-time
                  self_hosted_ica:
                    type: object
                    properties:
                      port_id:
                        type: string
                      connection_id:
                        type: string
                    title: base config for ICA
                  trustless_agent:
                    type: object
                    properties:
                      agent_address:
                        type: string
                      fee_limit:
                        type: array
                        items:
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                          description: >-
                            Coin defines a token with a denomination and an
                            amount.


                            NOTE: The amount field is an Int which implements
                            the custom method

                            signatures required by gogoproto.
                        title: >-
                          optional, if set to empty array, no fee limit is set
                          and no fees are charged
                      connection_id:
                        type: string
                        title: optional for display
                    title: >-
                      config for a trustless agent for flow execution on host
                      chain
                  configuration:
                    type: object
                    properties:
                      save_responses:
                        type: boolean
                        title: >-
                          if true, the flow response outputs are saved and can
                          be used in logic
                      updating_disabled:
                        type: boolean
                        title: if true, the flow is not updatable
                      stop_on_success:
                        type: boolean
                        title: >-
                          If true, will execute until we get a successful Flow,
                          if false/unset will

                          always execute
                      stop_on_failure:
                        type: boolean
                        title: >-
                          If true, will execute until successful Flow, if
                          false/unset will always

                          execute
                      stop_on_timeout:
                        type: boolean
                        title: If true, will stop if message times oiut
                      fallback_to_owner_balance:
                        type: boolean
                        title: >-
                          If true, owner account balance is used when trigger
                          account funds run out
                    title: >-
                      ExecutionConfiguration provides the execution-related
                      configuration of the

                      flow
                  conditions:
                    type: object
                    properties:
                      feedback_loops:
                        type: array
                        items:
                          type: object
                          properties:
                            flow_id:
                              type: string
                              format: uint64
                              title: >-
                                flow to get the latest response value from,
                                optional
                            response_index:
                              type: integer
                              format: int64
                              title: index of the responses
                            response_key:
                              type: string
                              title: for example "Amount"
                            msgs_index:
                              type: integer
                              format: int64
                              title: index of the msg to replace
                            msg_key:
                              type: string
                              title: >-
                                key of the message to replace (e.g.
                                Amount[0].Amount, FromAddress)
                            value_type:
                              type: string
                              title: >-
                                can be anything from sdk.Int, sdk.Coin,
                                sdk.Coins, string, []string, []sdk.Int
                            icq_config:
                              description: >-
                                bool calculate_difference = 7; //True: calculate
                                the difference with the previous value instead
                                of using the value directly.


                                config of ICQ to perform
                              type: object
                              properties:
                                connection_id:
                                  type: string
                                chain_id:
                                  type: string
                                timeout_policy:
                                  type: string
                                  enum:
                                    - REJECT_QUERY_RESPONSE
                                    - RETRY_QUERY_REQUEST
                                    - EXECUTE_QUERY_CALLBACK
                                  default: REJECT_QUERY_RESPONSE
                                timeout_duration:
                                  type: string
                                query_type:
                                  type: string
                                  title: e.g. store/bank/key store/staking/key
                                query_key:
                                  type: string
                                  title: >-
                                    key in the store that stores the query e.g.
                                    stakingtypes.GetValidatorKey(validatorAddressBz)
                                response:
                                  type: string
                                  format: byte
                                  title: should be reset after execution
                              title: config for using interchain queries
                          title: >-
                            Replace value with value from message or response
                            from another flow’s

                            latest output before execution
                        title: >-
                          Replace value with value from message or response from
                          another flow’s

                          latest output
                      comparisons:
                        type: array
                        items:
                          type: object
                          properties:
                            flow_id:
                              type: string
                              format: uint64
                              title: >-
                                get the latest response value from other flow,
                                optional
                            response_index:
                              type: integer
                              format: int64
                              title: index of the message response, optional
                            response_key:
                              type: string
                              title: e.g. Amount[0].Amount, FromAddress, optional
                            value_type:
                              type: string
                              title: >-
                                can be anything from sdk.Int, sdk.Coin,
                                sdk.Coins, string, []string, []sdk.Int
                            operator:
                              type: string
                              enum:
                                - EQUAL
                                - CONTAINS
                                - NOT_CONTAINS
                                - SMALLER_THAN
                                - LARGER_THAN
                                - GREATER_EQUAL
                                - LESS_EQUAL
                                - STARTS_WITH
                                - ENDS_WITH
                                - NOT_EQUAL
                              default: EQUAL
                              description: >-
                                Comparison operators that can be used for
                                various types.

                                 - EQUAL: Equality check (for all types)
                                 - CONTAINS: Contains check (for strings, arrays, etc.)
                                 - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                                 - SMALLER_THAN: Less than check (for numeric types)
                                 - LARGER_THAN: Greater than check (for numeric types)
                                 - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                                 - LESS_EQUAL: Less than or equal to check (for numeric types)
                                 - STARTS_WITH: Starts with check (for strings)
                                 - ENDS_WITH: Ends with check (for strings)
                                 - NOT_EQUAL: Not equal check (for all types)
                            operand:
                              type: string
                            icq_config:
                              description: >-
                                bool calculate_difference = 7; //True: Calculate
                                the difference with the previous value.


                                config of ICQ to perform
                              type: object
                              properties:
                                connection_id:
                                  type: string
                                chain_id:
                                  type: string
                                timeout_policy:
                                  type: string
                                  enum:
                                    - REJECT_QUERY_RESPONSE
                                    - RETRY_QUERY_REQUEST
                                    - EXECUTE_QUERY_CALLBACK
                                  default: REJECT_QUERY_RESPONSE
                                timeout_duration:
                                  type: string
                                query_type:
                                  type: string
                                  title: e.g. store/bank/key store/staking/key
                                query_key:
                                  type: string
                                  title: >-
                                    key in the store that stores the query e.g.
                                    stakingtypes.GetValidatorKey(validatorAddressBz)
                                response:
                                  type: string
                                  format: byte
                                  title: should be reset after execution
                              title: config for using interchain queries
                          title: >-
                            Comparison is checked on the response in JSON before
                            execution of

                            flow and outputs true or false
                        title: Comparison with response response value
                      stop_on_success_of:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: >-
                          optional array of dependent intents that when
                          executing succesfully, stops

                          execution
                      stop_on_failure_of:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: >-
                          optional array of dependent intents that when not
                          executing succesfully,

                          stops execution
                      skip_on_failure_of:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: >-
                          optional array of dependent intents that should be
                          executed succesfully

                          after their latest call before execution is allowed
                      skip_on_success_of:
                        type: array
                        items:
                          type: string
                          format: uint64
                        title: >-
                          optional array of dependent intents that should fail
                          after their latest

                          call before execution is allowed
                      use_and_for_comparisons:
                        type: boolean
                        description: >-
                          True: Use AND for combining comparisons. False: Use OR
                          for combining comparisons.
                    title: >-
                      ExecutionConditions provides execution conditions for the
                      flow
                title: Flow stores the info for the flow
            title: QueryFlowResponse the response type for the Query/FlowRequest RPC
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          in: path
          required: true
          type: string
      tags:
        - Query
  /intento/intent/v1/flows:
    get:
      summary: Flows returns the all flows
      operationId: Flows
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              flows:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    owner:
                      type: string
                    label:
                      type: string
                    fee_address:
                      type: string
                    msgs:
                      type: array
                      items:
                        type: object
                        properties:
                          '@type':
                            type: string
                            description: >-
                              A URL/resource name that uniquely identifies the
                              type of the serialized

                              protocol buffer message. This string must contain
                              at least

                              one "/" character. The last segment of the URL's
                              path must represent

                              the fully qualified name of the type (as in

                              `path/google.protobuf.Duration`). The name should
                              be in a canonical form

                              (e.g., leading "." is not accepted).


                              In practice, teams usually precompile into the
                              binary all types that they

                              expect it to use in the context of Any. However,
                              for URLs which use the

                              scheme `http`, `https`, or no scheme, one can
                              optionally set up a type

                              server that maps type URLs to message definitions
                              as follows:


                              * If no scheme is provided, `https` is assumed.

                              * An HTTP GET on the URL must yield a
                              [google.protobuf.Type][]
                                value in binary format, or produce an error.
                              * Applications are allowed to cache lookup results
                              based on the
                                URL, or have them precompiled into a binary to avoid any
                                lookup. Therefore, binary compatibility needs to be preserved
                                on changes to types. (Use versioned type names to manage
                                breaking changes.)

                              Note: this functionality is not currently
                              available in the official

                              protobuf release, and it is not used for type URLs
                              beginning with

                              type.googleapis.com. As of May 2023, there are no
                              widely used type server

                              implementations and no plans to implement one.


                              Schemes other than `http`, `https` (or the empty
                              scheme) might be

                              used with implementation specific semantics.
                        additionalProperties: {}
                        description: >-
                          `Any` contains an arbitrary serialized protocol buffer
                          message along with a

                          URL that describes the type of the serialized message.


                          Protobuf library provides support to pack/unpack Any
                          values in the form

                          of utility functions or additional generated methods
                          of the Any type.


                          Example 1: Pack and unpack a message in C++.

                              Foo foo = ...;
                              Any any;
                              any.PackFrom(foo);
                              ...
                              if (any.UnpackTo(&foo)) {
                                ...
                              }

                          Example 2: Pack and unpack a message in Java.

                              Foo foo = ...;
                              Any any = Any.pack(foo);
                              ...
                              if (any.is(Foo.class)) {
                                foo = any.unpack(Foo.class);
                              }
                              // or ...
                              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                                foo = any.unpack(Foo.getDefaultInstance());
                              }

                           Example 3: Pack and unpack a message in Python.

                              foo = Foo(...)
                              any = Any()
                              any.Pack(foo)
                              ...
                              if any.Is(Foo.DESCRIPTOR):
                                any.Unpack(foo)
                                ...

                           Example 4: Pack and unpack a message in Go

                               foo := &pb.Foo{...}
                               any, err := anypb.New(foo)
                               if err != nil {
                                 ...
                               }
                               ...
                               foo := &pb.Foo{}
                               if err := any.UnmarshalTo(foo); err != nil {
                                 ...
                               }

                          The pack methods provided by protobuf library will by
                          default use

                          'type.googleapis.com/full.type.name' as the type URL
                          and the unpack

                          methods only use the fully qualified type name after
                          the last '/'

                          in the type URL, for example "foo.bar.com/x/y.z" will
                          yield type

                          name "y.z".


                          JSON

                          ====

                          The JSON representation of an `Any` value uses the
                          regular

                          representation of the deserialized, embedded message,
                          with an

                          additional field `@type` which contains the type URL.
                          Example:

                              package google.profile;
                              message Person {
                                string first_name = 1;
                                string last_name = 2;
                              }

                              {
                                "@type": "type.googleapis.com/google.profile.Person",
                                "firstName": <string>,
                                "lastName": <string>
                              }

                          If the embedded message type is well-known and has a
                          custom JSON

                          representation, that representation will be embedded
                          adding a field

                          `value` which holds the custom JSON in addition to the
                          `@type`

                          field. Example (for message
                          [google.protobuf.Duration][]):

                              {
                                "@type": "type.googleapis.com/google.protobuf.Duration",
                                "value": "1.212s"
                              }
                    interval:
                      type: string
                    start_time:
                      type: string
                      format: date-time
                    exec_time:
                      type: string
                      format: date-time
                    end_time:
                      type: string
                      format: date-time
                    update_history:
                      type: array
                      items:
                        type: string
                        format: date-time
                    self_hosted_ica:
                      type: object
                      properties:
                        port_id:
                          type: string
                        connection_id:
                          type: string
                      title: base config for ICA
                    trustless_agent:
                      type: object
                      properties:
                        agent_address:
                          type: string
                        fee_limit:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          title: >-
                            optional, if set to empty array, no fee limit is set
                            and no fees are charged
                        connection_id:
                          type: string
                          title: optional for display
                      title: >-
                        config for a trustless agent for flow execution on host
                        chain
                    configuration:
                      type: object
                      properties:
                        save_responses:
                          type: boolean
                          title: >-
                            if true, the flow response outputs are saved and can
                            be used in logic
                        updating_disabled:
                          type: boolean
                          title: if true, the flow is not updatable
                        stop_on_success:
                          type: boolean
                          title: >-
                            If true, will execute until we get a successful
                            Flow, if false/unset will

                            always execute
                        stop_on_failure:
                          type: boolean
                          title: >-
                            If true, will execute until successful Flow, if
                            false/unset will always

                            execute
                        stop_on_timeout:
                          type: boolean
                          title: If true, will stop if message times oiut
                        fallback_to_owner_balance:
                          type: boolean
                          title: >-
                            If true, owner account balance is used when trigger
                            account funds run out
                      title: >-
                        ExecutionConfiguration provides the execution-related
                        configuration of the

                        flow
                    conditions:
                      type: object
                      properties:
                        feedback_loops:
                          type: array
                          items:
                            type: object
                            properties:
                              flow_id:
                                type: string
                                format: uint64
                                title: >-
                                  flow to get the latest response value from,
                                  optional
                              response_index:
                                type: integer
                                format: int64
                                title: index of the responses
                              response_key:
                                type: string
                                title: for example "Amount"
                              msgs_index:
                                type: integer
                                format: int64
                                title: index of the msg to replace
                              msg_key:
                                type: string
                                title: >-
                                  key of the message to replace (e.g.
                                  Amount[0].Amount, FromAddress)
                              value_type:
                                type: string
                                title: >-
                                  can be anything from sdk.Int, sdk.Coin,
                                  sdk.Coins, string, []string, []sdk.Int
                              icq_config:
                                description: >-
                                  bool calculate_difference = 7; //True:
                                  calculate the difference with the previous
                                  value instead of using the value directly.


                                  config of ICQ to perform
                                type: object
                                properties:
                                  connection_id:
                                    type: string
                                  chain_id:
                                    type: string
                                  timeout_policy:
                                    type: string
                                    enum:
                                      - REJECT_QUERY_RESPONSE
                                      - RETRY_QUERY_REQUEST
                                      - EXECUTE_QUERY_CALLBACK
                                    default: REJECT_QUERY_RESPONSE
                                  timeout_duration:
                                    type: string
                                  query_type:
                                    type: string
                                    title: e.g. store/bank/key store/staking/key
                                  query_key:
                                    type: string
                                    title: >-
                                      key in the store that stores the query
                                      e.g.
                                      stakingtypes.GetValidatorKey(validatorAddressBz)
                                  response:
                                    type: string
                                    format: byte
                                    title: should be reset after execution
                                title: config for using interchain queries
                            title: >-
                              Replace value with value from message or response
                              from another flow’s

                              latest output before execution
                          title: >-
                            Replace value with value from message or response
                            from another flow’s

                            latest output
                        comparisons:
                          type: array
                          items:
                            type: object
                            properties:
                              flow_id:
                                type: string
                                format: uint64
                                title: >-
                                  get the latest response value from other flow,
                                  optional
                              response_index:
                                type: integer
                                format: int64
                                title: index of the message response, optional
                              response_key:
                                type: string
                                title: e.g. Amount[0].Amount, FromAddress, optional
                              value_type:
                                type: string
                                title: >-
                                  can be anything from sdk.Int, sdk.Coin,
                                  sdk.Coins, string, []string, []sdk.Int
                              operator:
                                type: string
                                enum:
                                  - EQUAL
                                  - CONTAINS
                                  - NOT_CONTAINS
                                  - SMALLER_THAN
                                  - LARGER_THAN
                                  - GREATER_EQUAL
                                  - LESS_EQUAL
                                  - STARTS_WITH
                                  - ENDS_WITH
                                  - NOT_EQUAL
                                default: EQUAL
                                description: >-
                                  Comparison operators that can be used for
                                  various types.

                                   - EQUAL: Equality check (for all types)
                                   - CONTAINS: Contains check (for strings, arrays, etc.)
                                   - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                                   - SMALLER_THAN: Less than check (for numeric types)
                                   - LARGER_THAN: Greater than check (for numeric types)
                                   - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                                   - LESS_EQUAL: Less than or equal to check (for numeric types)
                                   - STARTS_WITH: Starts with check (for strings)
                                   - ENDS_WITH: Ends with check (for strings)
                                   - NOT_EQUAL: Not equal check (for all types)
                              operand:
                                type: string
                              icq_config:
                                description: >-
                                  bool calculate_difference = 7; //True:
                                  Calculate the difference with the previous
                                  value.


                                  config of ICQ to perform
                                type: object
                                properties:
                                  connection_id:
                                    type: string
                                  chain_id:
                                    type: string
                                  timeout_policy:
                                    type: string
                                    enum:
                                      - REJECT_QUERY_RESPONSE
                                      - RETRY_QUERY_REQUEST
                                      - EXECUTE_QUERY_CALLBACK
                                    default: REJECT_QUERY_RESPONSE
                                  timeout_duration:
                                    type: string
                                  query_type:
                                    type: string
                                    title: e.g. store/bank/key store/staking/key
                                  query_key:
                                    type: string
                                    title: >-
                                      key in the store that stores the query
                                      e.g.
                                      stakingtypes.GetValidatorKey(validatorAddressBz)
                                  response:
                                    type: string
                                    format: byte
                                    title: should be reset after execution
                                title: config for using interchain queries
                            title: >-
                              Comparison is checked on the response in JSON
                              before execution of

                              flow and outputs true or false
                          title: Comparison with response response value
                        stop_on_success_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that when
                            executing succesfully, stops

                            execution
                        stop_on_failure_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that when not
                            executing succesfully,

                            stops execution
                        skip_on_failure_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that should be
                            executed succesfully

                            after their latest call before execution is allowed
                        skip_on_success_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that should fail
                            after their latest

                            call before execution is allowed
                        use_and_for_comparisons:
                          type: boolean
                          description: >-
                            True: Use AND for combining comparisons. False: Use
                            OR for combining comparisons.
                      title: >-
                        ExecutionConditions provides execution conditions for
                        the flow
                  title: Flow stores the info for the flow
              pagination:
                description: Pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: >-
              QueryFlowsResponse the response type for the Query/FlowsRequest
              RPCπ
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /intento/intent/v1/flows/{owner}:
    get:
      summary: FlowsForOwner returns all flows for a given owner
      operationId: FlowsForOwner
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              flows:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    owner:
                      type: string
                    label:
                      type: string
                    fee_address:
                      type: string
                    msgs:
                      type: array
                      items:
                        type: object
                        properties:
                          '@type':
                            type: string
                            description: >-
                              A URL/resource name that uniquely identifies the
                              type of the serialized

                              protocol buffer message. This string must contain
                              at least

                              one "/" character. The last segment of the URL's
                              path must represent

                              the fully qualified name of the type (as in

                              `path/google.protobuf.Duration`). The name should
                              be in a canonical form

                              (e.g., leading "." is not accepted).


                              In practice, teams usually precompile into the
                              binary all types that they

                              expect it to use in the context of Any. However,
                              for URLs which use the

                              scheme `http`, `https`, or no scheme, one can
                              optionally set up a type

                              server that maps type URLs to message definitions
                              as follows:


                              * If no scheme is provided, `https` is assumed.

                              * An HTTP GET on the URL must yield a
                              [google.protobuf.Type][]
                                value in binary format, or produce an error.
                              * Applications are allowed to cache lookup results
                              based on the
                                URL, or have them precompiled into a binary to avoid any
                                lookup. Therefore, binary compatibility needs to be preserved
                                on changes to types. (Use versioned type names to manage
                                breaking changes.)

                              Note: this functionality is not currently
                              available in the official

                              protobuf release, and it is not used for type URLs
                              beginning with

                              type.googleapis.com. As of May 2023, there are no
                              widely used type server

                              implementations and no plans to implement one.


                              Schemes other than `http`, `https` (or the empty
                              scheme) might be

                              used with implementation specific semantics.
                        additionalProperties: {}
                        description: >-
                          `Any` contains an arbitrary serialized protocol buffer
                          message along with a

                          URL that describes the type of the serialized message.


                          Protobuf library provides support to pack/unpack Any
                          values in the form

                          of utility functions or additional generated methods
                          of the Any type.


                          Example 1: Pack and unpack a message in C++.

                              Foo foo = ...;
                              Any any;
                              any.PackFrom(foo);
                              ...
                              if (any.UnpackTo(&foo)) {
                                ...
                              }

                          Example 2: Pack and unpack a message in Java.

                              Foo foo = ...;
                              Any any = Any.pack(foo);
                              ...
                              if (any.is(Foo.class)) {
                                foo = any.unpack(Foo.class);
                              }
                              // or ...
                              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                                foo = any.unpack(Foo.getDefaultInstance());
                              }

                           Example 3: Pack and unpack a message in Python.

                              foo = Foo(...)
                              any = Any()
                              any.Pack(foo)
                              ...
                              if any.Is(Foo.DESCRIPTOR):
                                any.Unpack(foo)
                                ...

                           Example 4: Pack and unpack a message in Go

                               foo := &pb.Foo{...}
                               any, err := anypb.New(foo)
                               if err != nil {
                                 ...
                               }
                               ...
                               foo := &pb.Foo{}
                               if err := any.UnmarshalTo(foo); err != nil {
                                 ...
                               }

                          The pack methods provided by protobuf library will by
                          default use

                          'type.googleapis.com/full.type.name' as the type URL
                          and the unpack

                          methods only use the fully qualified type name after
                          the last '/'

                          in the type URL, for example "foo.bar.com/x/y.z" will
                          yield type

                          name "y.z".


                          JSON

                          ====

                          The JSON representation of an `Any` value uses the
                          regular

                          representation of the deserialized, embedded message,
                          with an

                          additional field `@type` which contains the type URL.
                          Example:

                              package google.profile;
                              message Person {
                                string first_name = 1;
                                string last_name = 2;
                              }

                              {
                                "@type": "type.googleapis.com/google.profile.Person",
                                "firstName": <string>,
                                "lastName": <string>
                              }

                          If the embedded message type is well-known and has a
                          custom JSON

                          representation, that representation will be embedded
                          adding a field

                          `value` which holds the custom JSON in addition to the
                          `@type`

                          field. Example (for message
                          [google.protobuf.Duration][]):

                              {
                                "@type": "type.googleapis.com/google.protobuf.Duration",
                                "value": "1.212s"
                              }
                    interval:
                      type: string
                    start_time:
                      type: string
                      format: date-time
                    exec_time:
                      type: string
                      format: date-time
                    end_time:
                      type: string
                      format: date-time
                    update_history:
                      type: array
                      items:
                        type: string
                        format: date-time
                    self_hosted_ica:
                      type: object
                      properties:
                        port_id:
                          type: string
                        connection_id:
                          type: string
                      title: base config for ICA
                    trustless_agent:
                      type: object
                      properties:
                        agent_address:
                          type: string
                        fee_limit:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          title: >-
                            optional, if set to empty array, no fee limit is set
                            and no fees are charged
                        connection_id:
                          type: string
                          title: optional for display
                      title: >-
                        config for a trustless agent for flow execution on host
                        chain
                    configuration:
                      type: object
                      properties:
                        save_responses:
                          type: boolean
                          title: >-
                            if true, the flow response outputs are saved and can
                            be used in logic
                        updating_disabled:
                          type: boolean
                          title: if true, the flow is not updatable
                        stop_on_success:
                          type: boolean
                          title: >-
                            If true, will execute until we get a successful
                            Flow, if false/unset will

                            always execute
                        stop_on_failure:
                          type: boolean
                          title: >-
                            If true, will execute until successful Flow, if
                            false/unset will always

                            execute
                        stop_on_timeout:
                          type: boolean
                          title: If true, will stop if message times oiut
                        fallback_to_owner_balance:
                          type: boolean
                          title: >-
                            If true, owner account balance is used when trigger
                            account funds run out
                      title: >-
                        ExecutionConfiguration provides the execution-related
                        configuration of the

                        flow
                    conditions:
                      type: object
                      properties:
                        feedback_loops:
                          type: array
                          items:
                            type: object
                            properties:
                              flow_id:
                                type: string
                                format: uint64
                                title: >-
                                  flow to get the latest response value from,
                                  optional
                              response_index:
                                type: integer
                                format: int64
                                title: index of the responses
                              response_key:
                                type: string
                                title: for example "Amount"
                              msgs_index:
                                type: integer
                                format: int64
                                title: index of the msg to replace
                              msg_key:
                                type: string
                                title: >-
                                  key of the message to replace (e.g.
                                  Amount[0].Amount, FromAddress)
                              value_type:
                                type: string
                                title: >-
                                  can be anything from sdk.Int, sdk.Coin,
                                  sdk.Coins, string, []string, []sdk.Int
                              icq_config:
                                description: >-
                                  bool calculate_difference = 7; //True:
                                  calculate the difference with the previous
                                  value instead of using the value directly.


                                  config of ICQ to perform
                                type: object
                                properties:
                                  connection_id:
                                    type: string
                                  chain_id:
                                    type: string
                                  timeout_policy:
                                    type: string
                                    enum:
                                      - REJECT_QUERY_RESPONSE
                                      - RETRY_QUERY_REQUEST
                                      - EXECUTE_QUERY_CALLBACK
                                    default: REJECT_QUERY_RESPONSE
                                  timeout_duration:
                                    type: string
                                  query_type:
                                    type: string
                                    title: e.g. store/bank/key store/staking/key
                                  query_key:
                                    type: string
                                    title: >-
                                      key in the store that stores the query
                                      e.g.
                                      stakingtypes.GetValidatorKey(validatorAddressBz)
                                  response:
                                    type: string
                                    format: byte
                                    title: should be reset after execution
                                title: config for using interchain queries
                            title: >-
                              Replace value with value from message or response
                              from another flow’s

                              latest output before execution
                          title: >-
                            Replace value with value from message or response
                            from another flow’s

                            latest output
                        comparisons:
                          type: array
                          items:
                            type: object
                            properties:
                              flow_id:
                                type: string
                                format: uint64
                                title: >-
                                  get the latest response value from other flow,
                                  optional
                              response_index:
                                type: integer
                                format: int64
                                title: index of the message response, optional
                              response_key:
                                type: string
                                title: e.g. Amount[0].Amount, FromAddress, optional
                              value_type:
                                type: string
                                title: >-
                                  can be anything from sdk.Int, sdk.Coin,
                                  sdk.Coins, string, []string, []sdk.Int
                              operator:
                                type: string
                                enum:
                                  - EQUAL
                                  - CONTAINS
                                  - NOT_CONTAINS
                                  - SMALLER_THAN
                                  - LARGER_THAN
                                  - GREATER_EQUAL
                                  - LESS_EQUAL
                                  - STARTS_WITH
                                  - ENDS_WITH
                                  - NOT_EQUAL
                                default: EQUAL
                                description: >-
                                  Comparison operators that can be used for
                                  various types.

                                   - EQUAL: Equality check (for all types)
                                   - CONTAINS: Contains check (for strings, arrays, etc.)
                                   - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                                   - SMALLER_THAN: Less than check (for numeric types)
                                   - LARGER_THAN: Greater than check (for numeric types)
                                   - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                                   - LESS_EQUAL: Less than or equal to check (for numeric types)
                                   - STARTS_WITH: Starts with check (for strings)
                                   - ENDS_WITH: Ends with check (for strings)
                                   - NOT_EQUAL: Not equal check (for all types)
                              operand:
                                type: string
                              icq_config:
                                description: >-
                                  bool calculate_difference = 7; //True:
                                  Calculate the difference with the previous
                                  value.


                                  config of ICQ to perform
                                type: object
                                properties:
                                  connection_id:
                                    type: string
                                  chain_id:
                                    type: string
                                  timeout_policy:
                                    type: string
                                    enum:
                                      - REJECT_QUERY_RESPONSE
                                      - RETRY_QUERY_REQUEST
                                      - EXECUTE_QUERY_CALLBACK
                                    default: REJECT_QUERY_RESPONSE
                                  timeout_duration:
                                    type: string
                                  query_type:
                                    type: string
                                    title: e.g. store/bank/key store/staking/key
                                  query_key:
                                    type: string
                                    title: >-
                                      key in the store that stores the query
                                      e.g.
                                      stakingtypes.GetValidatorKey(validatorAddressBz)
                                  response:
                                    type: string
                                    format: byte
                                    title: should be reset after execution
                                title: config for using interchain queries
                            title: >-
                              Comparison is checked on the response in JSON
                              before execution of

                              flow and outputs true or false
                          title: Comparison with response response value
                        stop_on_success_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that when
                            executing succesfully, stops

                            execution
                        stop_on_failure_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that when not
                            executing succesfully,

                            stops execution
                        skip_on_failure_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that should be
                            executed succesfully

                            after their latest call before execution is allowed
                        skip_on_success_of:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            optional array of dependent intents that should fail
                            after their latest

                            call before execution is allowed
                        use_and_for_comparisons:
                          type: boolean
                          description: >-
                            True: Use AND for combining comparisons. False: Use
                            OR for combining comparisons.
                      title: >-
                        ExecutionConditions provides execution conditions for
                        the flow
                  title: Flow stores the info for the flow
              pagination:
                description: Pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: |-
              QueryFlowsForOwnerResponse the response type for the
              Query/FlowsForOwnerRequest RPC
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: owner
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /intento/intent/v1/params:
    get:
      summary: Params returns the total set of Intent parameters.
      operationId: IntentParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  flow_funds_commission:
                    type: string
                    format: int64
                    title: >-
                      Commision rate percentage for returning remaining funds in
                      flow fee account after final execution, e.g. 100 = 1X, 250
                      = 250
                  flow_flex_fee_mul:
                    type: string
                    format: int64
                    title: >-
                      Multiplier to the flexible flow gas fee in promille, e.g.
                      1000 = 1X, 2500 = 2,5x
                  burn_fee_per_msg:
                    type: string
                    format: int64
                    title: Fixed burn fee per message execution to burn native denom
                  gas_fee_coins:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    title: >-
                      Array of denoms that can be used for fee payment together
                      with an amount
                  max_flow_duration:
                    type: string
                    title: Maximum period for a flow
                  min_flow_duration:
                    type: string
                    title: Minimum period for a flow
                  min_flow_interval:
                    type: string
                    title: Minimum period for a flow
                  connection_rewards:
                    type: array
                    items:
                      type: object
                      properties:
                        connection_id:
                          type: string
                          title: e.g. "connection-12"
                        relayer_rewards:
                          type: array
                          items:
                            type: string
                            format: int64
                          title: 'same format: 0=Low, 1=Medium, 2=High'
                    description: >-
                      relayer rewards in uinto for each message class
                      0=Low,1=Medium, 2=High.

                      Rewards are in uinto and topped up in the module account
                      by alloc module.
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Query
  /intento/intent/v1/trustless-agent/{agent_address}:
    get:
      summary: >-
        TrustlessAgent returns the trustless agent information for a given
        hosted address
      operationId: TrustlessAgent
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              trustless_agent:
                title: TrustlessAgent
                type: object
                properties:
                  agent_address:
                    type: string
                  ica_config:
                    type: object
                    properties:
                      port_id:
                        type: string
                      connection_id:
                        type: string
                    title: base config for ICA
                  fee_config:
                    type: object
                    properties:
                      fee_admin:
                        type: string
                        title: >-
                          can set fee coins supported, which can be charged per
                          message executed if a fee limit is set in the flow
                      fee_coins_supported:
                        type: array
                        items:
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                          description: >-
                            Coin defines a token with a denomination and an
                            amount.


                            NOTE: The amount field is an Int which implements
                            the custom method

                            signatures required by gogoproto.
                        title: can be empty
            description: >-
              QueryTrustlessAgentResponse is the response type for the
              Query/TrustlessAgent

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: agent_address
          in: path
          required: true
          type: string
      tags:
        - Query
  /intento/intent/v1/trustless-agents:
    get:
      summary: TrustlessAgents returns all trustless agents
      operationId: TrustlessAgents
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              trustless_agents:
                type: array
                items:
                  type: object
                  properties:
                    agent_address:
                      type: string
                    ica_config:
                      type: object
                      properties:
                        port_id:
                          type: string
                        connection_id:
                          type: string
                      title: base config for ICA
                    fee_config:
                      type: object
                      properties:
                        fee_admin:
                          type: string
                          title: >-
                            can set fee coins supported, which can be charged
                            per message executed if a fee limit is set in the
                            flow
                        fee_coins_supported:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          title: can be empty
                title: TrustlessAgents
              pagination:
                description: Pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryTrustlessAgentsResponse is the response type for the
              Query/TrustlessAgent

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /intento/intent/v1/trustless-agents/{fee_admin}:
    get:
      summary: >-
        TrustlessAgentsByFeeAdmin returns all trustless agents for a given fee
        admin address
      operationId: TrustlessAgentsByFeeAdmin
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              trustless_agents:
                type: array
                items:
                  type: object
                  properties:
                    agent_address:
                      type: string
                    ica_config:
                      type: object
                      properties:
                        port_id:
                          type: string
                        connection_id:
                          type: string
                      title: base config for ICA
                    fee_config:
                      type: object
                      properties:
                        fee_admin:
                          type: string
                          title: >-
                            can set fee coins supported, which can be charged
                            per message executed if a fee limit is set in the
                            flow
                        fee_coins_supported:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          title: can be empty
                title: TrustlessAgents
              pagination:
                description: Pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryTrustlessAgentResponse is the response type for the
              Query/TrustlessAgent

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: fee_admin
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /intento/interchainquery/pending_queries:
    get:
      operationId: PendingQueries
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              pending_queries:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    connection_id:
                      type: string
                    chain_id:
                      type: string
                    query_type:
                      type: string
                    request_data:
                      type: string
                      format: byte
                    callback_module:
                      type: string
                    callback_id:
                      type: string
                    callback_data:
                      type: string
                      format: byte
                    timeout_policy:
                      type: string
                      enum:
                        - REJECT_QUERY_RESPONSE
                        - RETRY_QUERY_REQUEST
                        - EXECUTE_QUERY_CALLBACK
                      default: REJECT_QUERY_RESPONSE
                    timeout_duration:
                      type: string
                    timeout_timestamp:
                      type: string
                      format: uint64
                    request_sent:
                      type: boolean
                    submission_height:
                      type: string
                      format: uint64
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - QueryService
  /mint/v1/annual_provisions:
    get:
      summary: AnnualProvisions current minting annual provision value.
      operationId: AnnualProvisions
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              annual_provisions:
                type: string
                format: byte
                description: >-
                  annual_provisions is the current minting annual provisions
                  value.
            description: |-
              QueryAnnualProvisionsResponse is the response type for the
              Query/AnnualProvisions RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Query
  /mint/v1/params:
    get:
      summary: Params returns the total set of minting parameters.
      operationId: MintParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  mint_denom:
                    type: string
                    title: type of coin to mint
                  start_time:
                    type: string
                    format: date-time
                    title: the time the chain starts
                  initial_annual_provisions:
                    type: string
                    title: initial annual provisions
                  reduction_factor:
                    type: string
                    title: factor to reduce inflation by each year
                  blocks_per_year:
                    type: string
                    format: uint64
                    title: expected blocks per year
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Query
definitions:
  cosmos.base.v1beta1.Coin:
    type: object
    properties:
      denom:
        type: string
      amount:
        type: string
    description: |-
      Coin defines a token with a denomination and an amount.

      NOTE: The amount field is an Int which implements the custom method
      signatures required by gogoproto.
  google.protobuf.Any:
    type: object
    properties:
      '@type':
        type: string
        description: >-
          A URL/resource name that uniquely identifies the type of the
          serialized

          protocol buffer message. This string must contain at least

          one "/" character. The last segment of the URL's path must represent

          the fully qualified name of the type (as in

          `path/google.protobuf.Duration`). The name should be in a canonical
          form

          (e.g., leading "." is not accepted).


          In practice, teams usually precompile into the binary all types that
          they

          expect it to use in the context of Any. However, for URLs which use
          the

          scheme `http`, `https`, or no scheme, one can optionally set up a type

          server that maps type URLs to message definitions as follows:


          * If no scheme is provided, `https` is assumed.

          * An HTTP GET on the URL must yield a [google.protobuf.Type][]
            value in binary format, or produce an error.
          * Applications are allowed to cache lookup results based on the
            URL, or have them precompiled into a binary to avoid any
            lookup. Therefore, binary compatibility needs to be preserved
            on changes to types. (Use versioned type names to manage
            breaking changes.)

          Note: this functionality is not currently available in the official

          protobuf release, and it is not used for type URLs beginning with

          type.googleapis.com. As of May 2023, there are no widely used type
          server

          implementations and no plans to implement one.


          Schemes other than `http`, `https` (or the empty scheme) might be

          used with implementation specific semantics.
    additionalProperties: {}
    description: >-
      `Any` contains an arbitrary serialized protocol buffer message along with
      a

      URL that describes the type of the serialized message.


      Protobuf library provides support to pack/unpack Any values in the form

      of utility functions or additional generated methods of the Any type.


      Example 1: Pack and unpack a message in C++.

          Foo foo = ...;
          Any any;
          any.PackFrom(foo);
          ...
          if (any.UnpackTo(&foo)) {
            ...
          }

      Example 2: Pack and unpack a message in Java.

          Foo foo = ...;
          Any any = Any.pack(foo);
          ...
          if (any.is(Foo.class)) {
            foo = any.unpack(Foo.class);
          }
          // or ...
          if (any.isSameTypeAs(Foo.getDefaultInstance())) {
            foo = any.unpack(Foo.getDefaultInstance());
          }

       Example 3: Pack and unpack a message in Python.

          foo = Foo(...)
          any = Any()
          any.Pack(foo)
          ...
          if any.Is(Foo.DESCRIPTOR):
            any.Unpack(foo)
            ...

       Example 4: Pack and unpack a message in Go

           foo := &pb.Foo{...}
           any, err := anypb.New(foo)
           if err != nil {
             ...
           }
           ...
           foo := &pb.Foo{}
           if err := any.UnmarshalTo(foo); err != nil {
             ...
           }

      The pack methods provided by protobuf library will by default use

      'type.googleapis.com/full.type.name' as the type URL and the unpack

      methods only use the fully qualified type name after the last '/'

      in the type URL, for example "foo.bar.com/x/y.z" will yield type

      name "y.z".


      JSON

      ====

      The JSON representation of an `Any` value uses the regular

      representation of the deserialized, embedded message, with an

      additional field `@type` which contains the type URL. Example:

          package google.profile;
          message Person {
            string first_name = 1;
            string last_name = 2;
          }

          {
            "@type": "type.googleapis.com/google.profile.Person",
            "firstName": <string>,
            "lastName": <string>
          }

      If the embedded message type is well-known and has a custom JSON

      representation, that representation will be embedded adding a field

      `value` which holds the custom JSON in addition to the `@type`

      field. Example (for message [google.protobuf.Duration][]):

          {
            "@type": "type.googleapis.com/google.protobuf.Duration",
            "value": "1.212s"
          }
  google.rpc.Status:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
  intento.alloc.v1.DistributionProportions:
    type: object
    properties:
      developer_rewards:
        type: string
      community_pool:
        type: string
      relayer_incentives:
        type: string
    description: DistributionProportions defines the proportion that each bucket  receives.
  intento.alloc.v1.Params:
    type: object
    properties:
      distribution_proportions:
        title: distribution_proportions defines the proportion of the minted denom
        type: object
        properties:
          developer_rewards:
            type: string
          community_pool:
            type: string
          relayer_incentives:
            type: string
        description: >-
          DistributionProportions defines the proportion that each bucket 
          receives.
      weighted_developer_rewards_receivers:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
            weight:
              type: string
          description: WeightedAddress defines an address with a weight.
        title: addresses to receive developer rewards
      supplement_amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          SupplementAmount is the amount to be supplemented from the pool on top
          of

          newly minted coins.
    description: Params defines the parameters for the alloc module.
  intento.alloc.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          distribution_proportions:
            title: >-
              distribution_proportions defines the proportion of the minted
              denom
            type: object
            properties:
              developer_rewards:
                type: string
              community_pool:
                type: string
              relayer_incentives:
                type: string
            description: >-
              DistributionProportions defines the proportion that each bucket 
              receives.
          weighted_developer_rewards_receivers:
            type: array
            items:
              type: object
              properties:
                address:
                  type: string
                weight:
                  type: string
              description: WeightedAddress defines an address with a weight.
            title: addresses to receive developer rewards
          supplement_amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              SupplementAmount is the amount to be supplemented from the pool on
              top of

              newly minted coins.
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  intento.alloc.v1.WeightedAddress:
    type: object
    properties:
      address:
        type: string
      weight:
        type: string
    description: WeightedAddress defines an address with a weight.
  intento.claim.v1.Action:
    type: string
    enum:
      - ACTION_ACTION_LOCAL
      - ACTION_ACTION_ICA
      - ACTION_GOVERNANCE_VOTE
      - ACTION_DELEGATE_STAKE
    default: ACTION_ACTION_LOCAL
  intento.claim.v1.ClaimRecord:
    type: object
    properties:
      address:
        type: string
        title: address of recipient
      maximum_claimable_amount:
        title: maximum claimable amount for the address
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
        description: |-
          Coin defines a token with a denomination and an amount.

          NOTE: The amount field is an Int which implements the custom method
          signatures required by gogoproto.
      status:
        type: array
        items:
          type: object
          properties:
            action_completed:
              type: boolean
              title: true if action is completed
            vesting_periods_completed:
              type: array
              items:
                type: boolean
              title: |-
                true if action is completed
                index refers to the 4 vesting periods for the given action
            vesting_periods_claimed:
              type: array
              items:
                type: boolean
              title: |-
                true if action is completed
                index refers to the 4 vesting periods for the given action
          title: Status contains for an action if it is completed and claimed
        title: 'index of status array refers to action enum #'
    title: A Claim Records is the metadata of claim data per address
  intento.claim.v1.Params:
    type: object
    properties:
      airdrop_start_time:
        type: string
        format: date-time
      duration_until_decay:
        type: string
      duration_of_decay:
        type: string
      claim_denom:
        type: string
        title: denom of claimable asset
      duration_vesting_periods:
        type: array
        items:
          type: string
    description: Params defines the claim module's parameters.
  intento.claim.v1.QueryClaimRecordResponse:
    type: object
    properties:
      claim_record:
        type: object
        properties:
          address:
            type: string
            title: address of recipient
          maximum_claimable_amount:
            title: maximum claimable amount for the address
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
            description: >-
              Coin defines a token with a denomination and an amount.


              NOTE: The amount field is an Int which implements the custom
              method

              signatures required by gogoproto.
          status:
            type: array
            items:
              type: object
              properties:
                action_completed:
                  type: boolean
                  title: true if action is completed
                vesting_periods_completed:
                  type: array
                  items:
                    type: boolean
                  title: |-
                    true if action is completed
                    index refers to the 4 vesting periods for the given action
                vesting_periods_claimed:
                  type: array
                  items:
                    type: boolean
                  title: |-
                    true if action is completed
                    index refers to the 4 vesting periods for the given action
              title: Status contains for an action if it is completed and claimed
            title: 'index of status array refers to action enum #'
        title: A Claim Records is the metadata of claim data per address
  intento.claim.v1.QueryClaimableForActionResponse:
    type: object
    properties:
      total:
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
        description: |-
          Coin defines a token with a denomination and an amount.

          NOTE: The amount field is an Int which implements the custom method
          signatures required by gogoproto.
  intento.claim.v1.QueryModuleAccountBalanceResponse:
    type: object
    properties:
      moduleAccountBalance:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: params defines the parameters of the module.
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  intento.claim.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          airdrop_start_time:
            type: string
            format: date-time
          duration_until_decay:
            type: string
          duration_of_decay:
            type: string
          claim_denom:
            type: string
            title: denom of claimable asset
          duration_vesting_periods:
            type: array
            items:
              type: string
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  intento.claim.v1.QueryTotalClaimableResponse:
    type: object
    properties:
      total:
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
        description: |-
          Coin defines a token with a denomination and an amount.

          NOTE: The amount field is an Int which implements the custom method
          signatures required by gogoproto.
  intento.claim.v1.Status:
    type: object
    properties:
      action_completed:
        type: boolean
        title: true if action is completed
      vesting_periods_completed:
        type: array
        items:
          type: boolean
        title: |-
          true if action is completed
          index refers to the 4 vesting periods for the given action
      vesting_periods_claimed:
        type: array
        items:
          type: boolean
        title: |-
          true if action is completed
          index refers to the 4 vesting periods for the given action
    title: Status contains for an action if it is completed and claimed
  cosmos.base.query.v1beta1.PageRequest:
    type: object
    properties:
      key:
        type: string
        format: byte
        description: |-
          key is a value returned in PageResponse.next_key to begin
          querying the next page most efficiently. Only one of offset or key
          should be set.
      offset:
        type: string
        format: uint64
        description: |-
          offset is a numeric offset that can be used when key is unavailable.
          It is less efficient than using key. Only one of offset or key should
          be set.
      limit:
        type: string
        format: uint64
        description: >-
          limit is the total number of results to be returned in the result
          page.

          If left empty it will default to a value to be set by each app.
      count_total:
        type: boolean
        description: >-
          count_total is set to true  to indicate that the result set should
          include

          a count of the total number of items available for pagination in UIs.

          count_total is only respected when offset is used. It is ignored when
          key

          is set.
      reverse:
        type: boolean
        description: >-
          reverse is set to true if results are to be returned in the descending
          order.


          Since: cosmos-sdk 0.43
    description: |-
      message SomeRequest {
               Foo some_parameter = 1;
               PageRequest pagination = 2;
       }
    title: |-
      PageRequest is to be embedded in gRPC request messages for efficient
      pagination. Ex:
  cosmos.base.query.v1beta1.PageResponse:
    type: object
    properties:
      next_key:
        type: string
        format: byte
        description: |-
          next_key is the key to be passed to PageRequest.key to
          query the next page most efficiently. It will be empty if
          there are no more results.
      total:
        type: string
        format: uint64
        title: |-
          total is total number of results available if PageRequest.count_total
          was set, its value is undefined otherwise
    description: |-
      PageResponse is to be embedded in gRPC response messages where the
      corresponding request message has used PageRequest.

       message SomeResponse {
               repeated Bar results = 1;
               PageResponse page = 2;
       }
  intento.intent.v1.Comparison:
    type: object
    properties:
      flow_id:
        type: string
        format: uint64
        title: get the latest response value from other flow, optional
      response_index:
        type: integer
        format: int64
        title: index of the message response, optional
      response_key:
        type: string
        title: e.g. Amount[0].Amount, FromAddress, optional
      value_type:
        type: string
        title: >-
          can be anything from sdk.Int, sdk.Coin, sdk.Coins, string, []string,
          []sdk.Int
      operator:
        type: string
        enum:
          - EQUAL
          - CONTAINS
          - NOT_CONTAINS
          - SMALLER_THAN
          - LARGER_THAN
          - GREATER_EQUAL
          - LESS_EQUAL
          - STARTS_WITH
          - ENDS_WITH
          - NOT_EQUAL
        default: EQUAL
        description: |-
          Comparison operators that can be used for various types.

           - EQUAL: Equality check (for all types)
           - CONTAINS: Contains check (for strings, arrays, etc.)
           - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
           - SMALLER_THAN: Less than check (for numeric types)
           - LARGER_THAN: Greater than check (for numeric types)
           - GREATER_EQUAL: Greater than or equal to check (for numeric types)
           - LESS_EQUAL: Less than or equal to check (for numeric types)
           - STARTS_WITH: Starts with check (for strings)
           - ENDS_WITH: Ends with check (for strings)
           - NOT_EQUAL: Not equal check (for all types)
      operand:
        type: string
      icq_config:
        description: >-
          bool calculate_difference = 7; //True: Calculate the difference with
          the previous value.


          config of ICQ to perform
        type: object
        properties:
          connection_id:
            type: string
          chain_id:
            type: string
          timeout_policy:
            type: string
            enum:
              - REJECT_QUERY_RESPONSE
              - RETRY_QUERY_REQUEST
              - EXECUTE_QUERY_CALLBACK
            default: REJECT_QUERY_RESPONSE
          timeout_duration:
            type: string
          query_type:
            type: string
            title: e.g. store/bank/key store/staking/key
          query_key:
            type: string
            title: >-
              key in the store that stores the query e.g.
              stakingtypes.GetValidatorKey(validatorAddressBz)
          response:
            type: string
            format: byte
            title: should be reset after execution
        title: config for using interchain queries
    title: |-
      Comparison is checked on the response in JSON before execution of
      flow and outputs true or false
  intento.intent.v1.ComparisonOperator:
    type: string
    enum:
      - EQUAL
      - CONTAINS
      - NOT_CONTAINS
      - SMALLER_THAN
      - LARGER_THAN
      - GREATER_EQUAL
      - LESS_EQUAL
      - STARTS_WITH
      - ENDS_WITH
      - NOT_EQUAL
    default: EQUAL
    description: |-
      Comparison operators that can be used for various types.

       - EQUAL: Equality check (for all types)
       - CONTAINS: Contains check (for strings, arrays, etc.)
       - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
       - SMALLER_THAN: Less than check (for numeric types)
       - LARGER_THAN: Greater than check (for numeric types)
       - GREATER_EQUAL: Greater than or equal to check (for numeric types)
       - LESS_EQUAL: Less than or equal to check (for numeric types)
       - STARTS_WITH: Starts with check (for strings)
       - ENDS_WITH: Ends with check (for strings)
       - NOT_EQUAL: Not equal check (for all types)
  intento.intent.v1.ConnectionRelayerReward:
    type: object
    properties:
      connection_id:
        type: string
        title: e.g. "connection-12"
      relayer_rewards:
        type: array
        items:
          type: string
          format: int64
        title: 'same format: 0=Low, 1=Medium, 2=High'
  intento.intent.v1.ExecutionConditions:
    type: object
    properties:
      feedback_loops:
        type: array
        items:
          type: object
          properties:
            flow_id:
              type: string
              format: uint64
              title: flow to get the latest response value from, optional
            response_index:
              type: integer
              format: int64
              title: index of the responses
            response_key:
              type: string
              title: for example "Amount"
            msgs_index:
              type: integer
              format: int64
              title: index of the msg to replace
            msg_key:
              type: string
              title: >-
                key of the message to replace (e.g. Amount[0].Amount,
                FromAddress)
            value_type:
              type: string
              title: >-
                can be anything from sdk.Int, sdk.Coin, sdk.Coins, string,
                []string, []sdk.Int
            icq_config:
              description: >-
                bool calculate_difference = 7; //True: calculate the difference
                with the previous value instead of using the value directly.


                config of ICQ to perform
              type: object
              properties:
                connection_id:
                  type: string
                chain_id:
                  type: string
                timeout_policy:
                  type: string
                  enum:
                    - REJECT_QUERY_RESPONSE
                    - RETRY_QUERY_REQUEST
                    - EXECUTE_QUERY_CALLBACK
                  default: REJECT_QUERY_RESPONSE
                timeout_duration:
                  type: string
                query_type:
                  type: string
                  title: e.g. store/bank/key store/staking/key
                query_key:
                  type: string
                  title: >-
                    key in the store that stores the query e.g.
                    stakingtypes.GetValidatorKey(validatorAddressBz)
                response:
                  type: string
                  format: byte
                  title: should be reset after execution
              title: config for using interchain queries
          title: >-
            Replace value with value from message or response from another
            flow’s

            latest output before execution
        title: |-
          Replace value with value from message or response from another flow’s
          latest output
      comparisons:
        type: array
        items:
          type: object
          properties:
            flow_id:
              type: string
              format: uint64
              title: get the latest response value from other flow, optional
            response_index:
              type: integer
              format: int64
              title: index of the message response, optional
            response_key:
              type: string
              title: e.g. Amount[0].Amount, FromAddress, optional
            value_type:
              type: string
              title: >-
                can be anything from sdk.Int, sdk.Coin, sdk.Coins, string,
                []string, []sdk.Int
            operator:
              type: string
              enum:
                - EQUAL
                - CONTAINS
                - NOT_CONTAINS
                - SMALLER_THAN
                - LARGER_THAN
                - GREATER_EQUAL
                - LESS_EQUAL
                - STARTS_WITH
                - ENDS_WITH
                - NOT_EQUAL
              default: EQUAL
              description: |-
                Comparison operators that can be used for various types.

                 - EQUAL: Equality check (for all types)
                 - CONTAINS: Contains check (for strings, arrays, etc.)
                 - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                 - SMALLER_THAN: Less than check (for numeric types)
                 - LARGER_THAN: Greater than check (for numeric types)
                 - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                 - LESS_EQUAL: Less than or equal to check (for numeric types)
                 - STARTS_WITH: Starts with check (for strings)
                 - ENDS_WITH: Ends with check (for strings)
                 - NOT_EQUAL: Not equal check (for all types)
            operand:
              type: string
            icq_config:
              description: >-
                bool calculate_difference = 7; //True: Calculate the difference
                with the previous value.


                config of ICQ to perform
              type: object
              properties:
                connection_id:
                  type: string
                chain_id:
                  type: string
                timeout_policy:
                  type: string
                  enum:
                    - REJECT_QUERY_RESPONSE
                    - RETRY_QUERY_REQUEST
                    - EXECUTE_QUERY_CALLBACK
                  default: REJECT_QUERY_RESPONSE
                timeout_duration:
                  type: string
                query_type:
                  type: string
                  title: e.g. store/bank/key store/staking/key
                query_key:
                  type: string
                  title: >-
                    key in the store that stores the query e.g.
                    stakingtypes.GetValidatorKey(validatorAddressBz)
                response:
                  type: string
                  format: byte
                  title: should be reset after execution
              title: config for using interchain queries
          title: |-
            Comparison is checked on the response in JSON before execution of
            flow and outputs true or false
        title: Comparison with response response value
      stop_on_success_of:
        type: array
        items:
          type: string
          format: uint64
        title: >-
          optional array of dependent intents that when executing succesfully,
          stops

          execution
      stop_on_failure_of:
        type: array
        items:
          type: string
          format: uint64
        title: >-
          optional array of dependent intents that when not executing
          succesfully,

          stops execution
      skip_on_failure_of:
        type: array
        items:
          type: string
          format: uint64
        title: >-
          optional array of dependent intents that should be executed
          succesfully

          after their latest call before execution is allowed
      skip_on_success_of:
        type: array
        items:
          type: string
          format: uint64
        title: >-
          optional array of dependent intents that should fail after their
          latest

          call before execution is allowed
      use_and_for_comparisons:
        type: boolean
        description: >-
          True: Use AND for combining comparisons. False: Use OR for combining
          comparisons.
    title: ExecutionConditions provides execution conditions for the flow
  intento.intent.v1.ExecutionConfiguration:
    type: object
    properties:
      save_responses:
        type: boolean
        title: if true, the flow response outputs are saved and can be used in logic
      updating_disabled:
        type: boolean
        title: if true, the flow is not updatable
      stop_on_success:
        type: boolean
        title: >-
          If true, will execute until we get a successful Flow, if false/unset
          will

          always execute
      stop_on_failure:
        type: boolean
        title: >-
          If true, will execute until successful Flow, if false/unset will
          always

          execute
      stop_on_timeout:
        type: boolean
        title: If true, will stop if message times oiut
      fallback_to_owner_balance:
        type: boolean
        title: >-
          If true, owner account balance is used when trigger account funds run
          out
    title: |-
      ExecutionConfiguration provides the execution-related configuration of the
      flow
  intento.intent.v1.FeedbackLoop:
    type: object
    properties:
      flow_id:
        type: string
        format: uint64
        title: flow to get the latest response value from, optional
      response_index:
        type: integer
        format: int64
        title: index of the responses
      response_key:
        type: string
        title: for example "Amount"
      msgs_index:
        type: integer
        format: int64
        title: index of the msg to replace
      msg_key:
        type: string
        title: key of the message to replace (e.g. Amount[0].Amount, FromAddress)
      value_type:
        type: string
        title: >-
          can be anything from sdk.Int, sdk.Coin, sdk.Coins, string, []string,
          []sdk.Int
      icq_config:
        description: >-
          bool calculate_difference = 7; //True: calculate the difference with
          the previous value instead of using the value directly.


          config of ICQ to perform
        type: object
        properties:
          connection_id:
            type: string
          chain_id:
            type: string
          timeout_policy:
            type: string
            enum:
              - REJECT_QUERY_RESPONSE
              - RETRY_QUERY_REQUEST
              - EXECUTE_QUERY_CALLBACK
            default: REJECT_QUERY_RESPONSE
          timeout_duration:
            type: string
          query_type:
            type: string
            title: e.g. store/bank/key store/staking/key
          query_key:
            type: string
            title: >-
              key in the store that stores the query e.g.
              stakingtypes.GetValidatorKey(validatorAddressBz)
          response:
            type: string
            format: byte
            title: should be reset after execution
        title: config for using interchain queries
    title: |-
      Replace value with value from message or response from another flow’s
      latest output before execution
  intento.intent.v1.Flow:
    type: object
    properties:
      id:
        type: string
        format: uint64
      owner:
        type: string
      label:
        type: string
      fee_address:
        type: string
      msgs:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
      interval:
        type: string
      start_time:
        type: string
        format: date-time
      exec_time:
        type: string
        format: date-time
      end_time:
        type: string
        format: date-time
      update_history:
        type: array
        items:
          type: string
          format: date-time
      self_hosted_ica:
        type: object
        properties:
          port_id:
            type: string
          connection_id:
            type: string
        title: base config for ICA
      trustless_agent:
        type: object
        properties:
          agent_address:
            type: string
          fee_limit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            title: >-
              optional, if set to empty array, no fee limit is set and no fees
              are charged
          connection_id:
            type: string
            title: optional for display
        title: config for a trustless agent for flow execution on host chain
      configuration:
        type: object
        properties:
          save_responses:
            type: boolean
            title: >-
              if true, the flow response outputs are saved and can be used in
              logic
          updating_disabled:
            type: boolean
            title: if true, the flow is not updatable
          stop_on_success:
            type: boolean
            title: >-
              If true, will execute until we get a successful Flow, if
              false/unset will

              always execute
          stop_on_failure:
            type: boolean
            title: >-
              If true, will execute until successful Flow, if false/unset will
              always

              execute
          stop_on_timeout:
            type: boolean
            title: If true, will stop if message times oiut
          fallback_to_owner_balance:
            type: boolean
            title: >-
              If true, owner account balance is used when trigger account funds
              run out
        title: >-
          ExecutionConfiguration provides the execution-related configuration of
          the

          flow
      conditions:
        type: object
        properties:
          feedback_loops:
            type: array
            items:
              type: object
              properties:
                flow_id:
                  type: string
                  format: uint64
                  title: flow to get the latest response value from, optional
                response_index:
                  type: integer
                  format: int64
                  title: index of the responses
                response_key:
                  type: string
                  title: for example "Amount"
                msgs_index:
                  type: integer
                  format: int64
                  title: index of the msg to replace
                msg_key:
                  type: string
                  title: >-
                    key of the message to replace (e.g. Amount[0].Amount,
                    FromAddress)
                value_type:
                  type: string
                  title: >-
                    can be anything from sdk.Int, sdk.Coin, sdk.Coins, string,
                    []string, []sdk.Int
                icq_config:
                  description: >-
                    bool calculate_difference = 7; //True: calculate the
                    difference with the previous value instead of using the
                    value directly.


                    config of ICQ to perform
                  type: object
                  properties:
                    connection_id:
                      type: string
                    chain_id:
                      type: string
                    timeout_policy:
                      type: string
                      enum:
                        - REJECT_QUERY_RESPONSE
                        - RETRY_QUERY_REQUEST
                        - EXECUTE_QUERY_CALLBACK
                      default: REJECT_QUERY_RESPONSE
                    timeout_duration:
                      type: string
                    query_type:
                      type: string
                      title: e.g. store/bank/key store/staking/key
                    query_key:
                      type: string
                      title: >-
                        key in the store that stores the query e.g.
                        stakingtypes.GetValidatorKey(validatorAddressBz)
                    response:
                      type: string
                      format: byte
                      title: should be reset after execution
                  title: config for using interchain queries
              title: >-
                Replace value with value from message or response from another
                flow’s

                latest output before execution
            title: >-
              Replace value with value from message or response from another
              flow’s

              latest output
          comparisons:
            type: array
            items:
              type: object
              properties:
                flow_id:
                  type: string
                  format: uint64
                  title: get the latest response value from other flow, optional
                response_index:
                  type: integer
                  format: int64
                  title: index of the message response, optional
                response_key:
                  type: string
                  title: e.g. Amount[0].Amount, FromAddress, optional
                value_type:
                  type: string
                  title: >-
                    can be anything from sdk.Int, sdk.Coin, sdk.Coins, string,
                    []string, []sdk.Int
                operator:
                  type: string
                  enum:
                    - EQUAL
                    - CONTAINS
                    - NOT_CONTAINS
                    - SMALLER_THAN
                    - LARGER_THAN
                    - GREATER_EQUAL
                    - LESS_EQUAL
                    - STARTS_WITH
                    - ENDS_WITH
                    - NOT_EQUAL
                  default: EQUAL
                  description: |-
                    Comparison operators that can be used for various types.

                     - EQUAL: Equality check (for all types)
                     - CONTAINS: Contains check (for strings, arrays, etc.)
                     - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                     - SMALLER_THAN: Less than check (for numeric types)
                     - LARGER_THAN: Greater than check (for numeric types)
                     - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                     - LESS_EQUAL: Less than or equal to check (for numeric types)
                     - STARTS_WITH: Starts with check (for strings)
                     - ENDS_WITH: Ends with check (for strings)
                     - NOT_EQUAL: Not equal check (for all types)
                operand:
                  type: string
                icq_config:
                  description: >-
                    bool calculate_difference = 7; //True: Calculate the
                    difference with the previous value.


                    config of ICQ to perform
                  type: object
                  properties:
                    connection_id:
                      type: string
                    chain_id:
                      type: string
                    timeout_policy:
                      type: string
                      enum:
                        - REJECT_QUERY_RESPONSE
                        - RETRY_QUERY_REQUEST
                        - EXECUTE_QUERY_CALLBACK
                      default: REJECT_QUERY_RESPONSE
                    timeout_duration:
                      type: string
                    query_type:
                      type: string
                      title: e.g. store/bank/key store/staking/key
                    query_key:
                      type: string
                      title: >-
                        key in the store that stores the query e.g.
                        stakingtypes.GetValidatorKey(validatorAddressBz)
                    response:
                      type: string
                      format: byte
                      title: should be reset after execution
                  title: config for using interchain queries
              title: >-
                Comparison is checked on the response in JSON before execution
                of

                flow and outputs true or false
            title: Comparison with response response value
          stop_on_success_of:
            type: array
            items:
              type: string
              format: uint64
            title: >-
              optional array of dependent intents that when executing
              succesfully, stops

              execution
          stop_on_failure_of:
            type: array
            items:
              type: string
              format: uint64
            title: >-
              optional array of dependent intents that when not executing
              succesfully,

              stops execution
          skip_on_failure_of:
            type: array
            items:
              type: string
              format: uint64
            title: >-
              optional array of dependent intents that should be executed
              succesfully

              after their latest call before execution is allowed
          skip_on_success_of:
            type: array
            items:
              type: string
              format: uint64
            title: >-
              optional array of dependent intents that should fail after their
              latest

              call before execution is allowed
          use_and_for_comparisons:
            type: boolean
            description: >-
              True: Use AND for combining comparisons. False: Use OR for
              combining comparisons.
        title: ExecutionConditions provides execution conditions for the flow
    title: Flow stores the info for the flow
  intento.intent.v1.FlowHistoryEntry:
    type: object
    properties:
      scheduled_exec_time:
        type: string
        format: date-time
      actual_exec_time:
        type: string
        format: date-time
      exec_fee:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
      executed:
        type: boolean
        title: whether all messages are executed, independent of succesfull result
      timed_out:
        type: boolean
        title: timed out from execution over IBC
      errors:
        type: array
        items:
          type: string
        title: |-
          errors from execution, if executed and no error the execution was
          succesfull
      msg_responses:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        title: will be empty when save_responses is false
      query_responses:
        type: array
        items:
          type: string
        title: will be empty when save_responses is false
      packet_sequences:
        type: array
        items:
          type: string
          format: uint64
        title: packet sequences of the flow execution on the host chain
    title: FlowHistoryEntry provides a the history of flow interchain tx call
  intento.intent.v1.ICAConfig:
    type: object
    properties:
      port_id:
        type: string
      connection_id:
        type: string
    title: base config for ICA
  intento.intent.v1.ICQConfig:
    type: object
    properties:
      connection_id:
        type: string
      chain_id:
        type: string
      timeout_policy:
        type: string
        enum:
          - REJECT_QUERY_RESPONSE
          - RETRY_QUERY_REQUEST
          - EXECUTE_QUERY_CALLBACK
        default: REJECT_QUERY_RESPONSE
      timeout_duration:
        type: string
      query_type:
        type: string
        title: e.g. store/bank/key store/staking/key
      query_key:
        type: string
        title: >-
          key in the store that stores the query e.g.
          stakingtypes.GetValidatorKey(validatorAddressBz)
      response:
        type: string
        format: byte
        title: should be reset after execution
    title: config for using interchain queries
  intento.intent.v1.Params:
    type: object
    properties:
      flow_funds_commission:
        type: string
        format: int64
        title: >-
          Commision rate percentage for returning remaining funds in flow fee
          account after final execution, e.g. 100 = 1X, 250 = 250
      flow_flex_fee_mul:
        type: string
        format: int64
        title: >-
          Multiplier to the flexible flow gas fee in promille, e.g. 1000 = 1X,
          2500 = 2,5x
      burn_fee_per_msg:
        type: string
        format: int64
        title: Fixed burn fee per message execution to burn native denom
      gas_fee_coins:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: >-
          Array of denoms that can be used for fee payment together with an
          amount
      max_flow_duration:
        type: string
        title: Maximum period for a flow
      min_flow_duration:
        type: string
        title: Minimum period for a flow
      min_flow_interval:
        type: string
        title: Minimum period for a flow
      connection_rewards:
        type: array
        items:
          type: object
          properties:
            connection_id:
              type: string
              title: e.g. "connection-12"
            relayer_rewards:
              type: array
              items:
                type: string
                format: int64
              title: 'same format: 0=Low, 1=Medium, 2=High'
        description: >-
          relayer rewards in uinto for each message class 0=Low,1=Medium,
          2=High.

          Rewards are in uinto and topped up in the module account by alloc
          module.
    description: Params defines the params for activeness of Flows on governance proposals.
  intento.intent.v1.QueryFlowHistoryResponse:
    type: object
    properties:
      history:
        type: array
        items:
          type: object
          properties:
            scheduled_exec_time:
              type: string
              format: date-time
            actual_exec_time:
              type: string
              format: date-time
            exec_fee:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
            executed:
              type: boolean
              title: >-
                whether all messages are executed, independent of succesfull
                result
            timed_out:
              type: boolean
              title: timed out from execution over IBC
            errors:
              type: array
              items:
                type: string
              title: >-
                errors from execution, if executed and no error the execution
                was

                succesfull
            msg_responses:
              type: array
              items:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }
                      // or ...
                      if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                        foo = any.unpack(Foo.getDefaultInstance());
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := anypb.New(foo)
                       if err != nil {
                         ...
                       }
                       ...
                       foo := &pb.Foo{}
                       if err := any.UnmarshalTo(foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".


                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
              title: will be empty when save_responses is false
            query_responses:
              type: array
              items:
                type: string
              title: will be empty when save_responses is false
            packet_sequences:
              type: array
              items:
                type: string
                format: uint64
              title: packet sequences of the flow execution on the host chain
          title: FlowHistoryEntry provides a the history of flow interchain tx call
      pagination:
        description: Pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: |-
      QueryFlowHistoryResponse the response type for the
      Query/QueryFlowHistoryResponse RPC
  intento.intent.v1.QueryFlowResponse:
    type: object
    properties:
      flow:
        type: object
        properties:
          id:
            type: string
            format: uint64
          owner:
            type: string
          label:
            type: string
          fee_address:
            type: string
          msgs:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
          interval:
            type: string
          start_time:
            type: string
            format: date-time
          exec_time:
            type: string
            format: date-time
          end_time:
            type: string
            format: date-time
          update_history:
            type: array
            items:
              type: string
              format: date-time
          self_hosted_ica:
            type: object
            properties:
              port_id:
                type: string
              connection_id:
                type: string
            title: base config for ICA
          trustless_agent:
            type: object
            properties:
              agent_address:
                type: string
              fee_limit:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                title: >-
                  optional, if set to empty array, no fee limit is set and no
                  fees are charged
              connection_id:
                type: string
                title: optional for display
            title: config for a trustless agent for flow execution on host chain
          configuration:
            type: object
            properties:
              save_responses:
                type: boolean
                title: >-
                  if true, the flow response outputs are saved and can be used
                  in logic
              updating_disabled:
                type: boolean
                title: if true, the flow is not updatable
              stop_on_success:
                type: boolean
                title: >-
                  If true, will execute until we get a successful Flow, if
                  false/unset will

                  always execute
              stop_on_failure:
                type: boolean
                title: >-
                  If true, will execute until successful Flow, if false/unset
                  will always

                  execute
              stop_on_timeout:
                type: boolean
                title: If true, will stop if message times oiut
              fallback_to_owner_balance:
                type: boolean
                title: >-
                  If true, owner account balance is used when trigger account
                  funds run out
            title: >-
              ExecutionConfiguration provides the execution-related
              configuration of the

              flow
          conditions:
            type: object
            properties:
              feedback_loops:
                type: array
                items:
                  type: object
                  properties:
                    flow_id:
                      type: string
                      format: uint64
                      title: flow to get the latest response value from, optional
                    response_index:
                      type: integer
                      format: int64
                      title: index of the responses
                    response_key:
                      type: string
                      title: for example "Amount"
                    msgs_index:
                      type: integer
                      format: int64
                      title: index of the msg to replace
                    msg_key:
                      type: string
                      title: >-
                        key of the message to replace (e.g. Amount[0].Amount,
                        FromAddress)
                    value_type:
                      type: string
                      title: >-
                        can be anything from sdk.Int, sdk.Coin, sdk.Coins,
                        string, []string, []sdk.Int
                    icq_config:
                      description: >-
                        bool calculate_difference = 7; //True: calculate the
                        difference with the previous value instead of using the
                        value directly.


                        config of ICQ to perform
                      type: object
                      properties:
                        connection_id:
                          type: string
                        chain_id:
                          type: string
                        timeout_policy:
                          type: string
                          enum:
                            - REJECT_QUERY_RESPONSE
                            - RETRY_QUERY_REQUEST
                            - EXECUTE_QUERY_CALLBACK
                          default: REJECT_QUERY_RESPONSE
                        timeout_duration:
                          type: string
                        query_type:
                          type: string
                          title: e.g. store/bank/key store/staking/key
                        query_key:
                          type: string
                          title: >-
                            key in the store that stores the query e.g.
                            stakingtypes.GetValidatorKey(validatorAddressBz)
                        response:
                          type: string
                          format: byte
                          title: should be reset after execution
                      title: config for using interchain queries
                  title: >-
                    Replace value with value from message or response from
                    another flow’s

                    latest output before execution
                title: >-
                  Replace value with value from message or response from another
                  flow’s

                  latest output
              comparisons:
                type: array
                items:
                  type: object
                  properties:
                    flow_id:
                      type: string
                      format: uint64
                      title: get the latest response value from other flow, optional
                    response_index:
                      type: integer
                      format: int64
                      title: index of the message response, optional
                    response_key:
                      type: string
                      title: e.g. Amount[0].Amount, FromAddress, optional
                    value_type:
                      type: string
                      title: >-
                        can be anything from sdk.Int, sdk.Coin, sdk.Coins,
                        string, []string, []sdk.Int
                    operator:
                      type: string
                      enum:
                        - EQUAL
                        - CONTAINS
                        - NOT_CONTAINS
                        - SMALLER_THAN
                        - LARGER_THAN
                        - GREATER_EQUAL
                        - LESS_EQUAL
                        - STARTS_WITH
                        - ENDS_WITH
                        - NOT_EQUAL
                      default: EQUAL
                      description: |-
                        Comparison operators that can be used for various types.

                         - EQUAL: Equality check (for all types)
                         - CONTAINS: Contains check (for strings, arrays, etc.)
                         - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                         - SMALLER_THAN: Less than check (for numeric types)
                         - LARGER_THAN: Greater than check (for numeric types)
                         - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                         - LESS_EQUAL: Less than or equal to check (for numeric types)
                         - STARTS_WITH: Starts with check (for strings)
                         - ENDS_WITH: Ends with check (for strings)
                         - NOT_EQUAL: Not equal check (for all types)
                    operand:
                      type: string
                    icq_config:
                      description: >-
                        bool calculate_difference = 7; //True: Calculate the
                        difference with the previous value.


                        config of ICQ to perform
                      type: object
                      properties:
                        connection_id:
                          type: string
                        chain_id:
                          type: string
                        timeout_policy:
                          type: string
                          enum:
                            - REJECT_QUERY_RESPONSE
                            - RETRY_QUERY_REQUEST
                            - EXECUTE_QUERY_CALLBACK
                          default: REJECT_QUERY_RESPONSE
                        timeout_duration:
                          type: string
                        query_type:
                          type: string
                          title: e.g. store/bank/key store/staking/key
                        query_key:
                          type: string
                          title: >-
                            key in the store that stores the query e.g.
                            stakingtypes.GetValidatorKey(validatorAddressBz)
                        response:
                          type: string
                          format: byte
                          title: should be reset after execution
                      title: config for using interchain queries
                  title: >-
                    Comparison is checked on the response in JSON before
                    execution of

                    flow and outputs true or false
                title: Comparison with response response value
              stop_on_success_of:
                type: array
                items:
                  type: string
                  format: uint64
                title: >-
                  optional array of dependent intents that when executing
                  succesfully, stops

                  execution
              stop_on_failure_of:
                type: array
                items:
                  type: string
                  format: uint64
                title: >-
                  optional array of dependent intents that when not executing
                  succesfully,

                  stops execution
              skip_on_failure_of:
                type: array
                items:
                  type: string
                  format: uint64
                title: >-
                  optional array of dependent intents that should be executed
                  succesfully

                  after their latest call before execution is allowed
              skip_on_success_of:
                type: array
                items:
                  type: string
                  format: uint64
                title: >-
                  optional array of dependent intents that should fail after
                  their latest

                  call before execution is allowed
              use_and_for_comparisons:
                type: boolean
                description: >-
                  True: Use AND for combining comparisons. False: Use OR for
                  combining comparisons.
            title: ExecutionConditions provides execution conditions for the flow
        title: Flow stores the info for the flow
    title: QueryFlowResponse the response type for the Query/FlowRequest RPC
  intento.intent.v1.QueryFlowsForOwnerResponse:
    type: object
    properties:
      flows:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            owner:
              type: string
            label:
              type: string
            fee_address:
              type: string
            msgs:
              type: array
              items:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }
                      // or ...
                      if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                        foo = any.unpack(Foo.getDefaultInstance());
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := anypb.New(foo)
                       if err != nil {
                         ...
                       }
                       ...
                       foo := &pb.Foo{}
                       if err := any.UnmarshalTo(foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".


                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
            interval:
              type: string
            start_time:
              type: string
              format: date-time
            exec_time:
              type: string
              format: date-time
            end_time:
              type: string
              format: date-time
            update_history:
              type: array
              items:
                type: string
                format: date-time
            self_hosted_ica:
              type: object
              properties:
                port_id:
                  type: string
                connection_id:
                  type: string
              title: base config for ICA
            trustless_agent:
              type: object
              properties:
                agent_address:
                  type: string
                fee_limit:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  title: >-
                    optional, if set to empty array, no fee limit is set and no
                    fees are charged
                connection_id:
                  type: string
                  title: optional for display
              title: config for a trustless agent for flow execution on host chain
            configuration:
              type: object
              properties:
                save_responses:
                  type: boolean
                  title: >-
                    if true, the flow response outputs are saved and can be used
                    in logic
                updating_disabled:
                  type: boolean
                  title: if true, the flow is not updatable
                stop_on_success:
                  type: boolean
                  title: >-
                    If true, will execute until we get a successful Flow, if
                    false/unset will

                    always execute
                stop_on_failure:
                  type: boolean
                  title: >-
                    If true, will execute until successful Flow, if false/unset
                    will always

                    execute
                stop_on_timeout:
                  type: boolean
                  title: If true, will stop if message times oiut
                fallback_to_owner_balance:
                  type: boolean
                  title: >-
                    If true, owner account balance is used when trigger account
                    funds run out
              title: >-
                ExecutionConfiguration provides the execution-related
                configuration of the

                flow
            conditions:
              type: object
              properties:
                feedback_loops:
                  type: array
                  items:
                    type: object
                    properties:
                      flow_id:
                        type: string
                        format: uint64
                        title: flow to get the latest response value from, optional
                      response_index:
                        type: integer
                        format: int64
                        title: index of the responses
                      response_key:
                        type: string
                        title: for example "Amount"
                      msgs_index:
                        type: integer
                        format: int64
                        title: index of the msg to replace
                      msg_key:
                        type: string
                        title: >-
                          key of the message to replace (e.g. Amount[0].Amount,
                          FromAddress)
                      value_type:
                        type: string
                        title: >-
                          can be anything from sdk.Int, sdk.Coin, sdk.Coins,
                          string, []string, []sdk.Int
                      icq_config:
                        description: >-
                          bool calculate_difference = 7; //True: calculate the
                          difference with the previous value instead of using
                          the value directly.


                          config of ICQ to perform
                        type: object
                        properties:
                          connection_id:
                            type: string
                          chain_id:
                            type: string
                          timeout_policy:
                            type: string
                            enum:
                              - REJECT_QUERY_RESPONSE
                              - RETRY_QUERY_REQUEST
                              - EXECUTE_QUERY_CALLBACK
                            default: REJECT_QUERY_RESPONSE
                          timeout_duration:
                            type: string
                          query_type:
                            type: string
                            title: e.g. store/bank/key store/staking/key
                          query_key:
                            type: string
                            title: >-
                              key in the store that stores the query e.g.
                              stakingtypes.GetValidatorKey(validatorAddressBz)
                          response:
                            type: string
                            format: byte
                            title: should be reset after execution
                        title: config for using interchain queries
                    title: >-
                      Replace value with value from message or response from
                      another flow’s

                      latest output before execution
                  title: >-
                    Replace value with value from message or response from
                    another flow’s

                    latest output
                comparisons:
                  type: array
                  items:
                    type: object
                    properties:
                      flow_id:
                        type: string
                        format: uint64
                        title: >-
                          get the latest response value from other flow,
                          optional
                      response_index:
                        type: integer
                        format: int64
                        title: index of the message response, optional
                      response_key:
                        type: string
                        title: e.g. Amount[0].Amount, FromAddress, optional
                      value_type:
                        type: string
                        title: >-
                          can be anything from sdk.Int, sdk.Coin, sdk.Coins,
                          string, []string, []sdk.Int
                      operator:
                        type: string
                        enum:
                          - EQUAL
                          - CONTAINS
                          - NOT_CONTAINS
                          - SMALLER_THAN
                          - LARGER_THAN
                          - GREATER_EQUAL
                          - LESS_EQUAL
                          - STARTS_WITH
                          - ENDS_WITH
                          - NOT_EQUAL
                        default: EQUAL
                        description: >-
                          Comparison operators that can be used for various
                          types.

                           - EQUAL: Equality check (for all types)
                           - CONTAINS: Contains check (for strings, arrays, etc.)
                           - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                           - SMALLER_THAN: Less than check (for numeric types)
                           - LARGER_THAN: Greater than check (for numeric types)
                           - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                           - LESS_EQUAL: Less than or equal to check (for numeric types)
                           - STARTS_WITH: Starts with check (for strings)
                           - ENDS_WITH: Ends with check (for strings)
                           - NOT_EQUAL: Not equal check (for all types)
                      operand:
                        type: string
                      icq_config:
                        description: >-
                          bool calculate_difference = 7; //True: Calculate the
                          difference with the previous value.


                          config of ICQ to perform
                        type: object
                        properties:
                          connection_id:
                            type: string
                          chain_id:
                            type: string
                          timeout_policy:
                            type: string
                            enum:
                              - REJECT_QUERY_RESPONSE
                              - RETRY_QUERY_REQUEST
                              - EXECUTE_QUERY_CALLBACK
                            default: REJECT_QUERY_RESPONSE
                          timeout_duration:
                            type: string
                          query_type:
                            type: string
                            title: e.g. store/bank/key store/staking/key
                          query_key:
                            type: string
                            title: >-
                              key in the store that stores the query e.g.
                              stakingtypes.GetValidatorKey(validatorAddressBz)
                          response:
                            type: string
                            format: byte
                            title: should be reset after execution
                        title: config for using interchain queries
                    title: >-
                      Comparison is checked on the response in JSON before
                      execution of

                      flow and outputs true or false
                  title: Comparison with response response value
                stop_on_success_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that when executing
                    succesfully, stops

                    execution
                stop_on_failure_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that when not executing
                    succesfully,

                    stops execution
                skip_on_failure_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that should be executed
                    succesfully

                    after their latest call before execution is allowed
                skip_on_success_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that should fail after
                    their latest

                    call before execution is allowed
                use_and_for_comparisons:
                  type: boolean
                  description: >-
                    True: Use AND for combining comparisons. False: Use OR for
                    combining comparisons.
              title: ExecutionConditions provides execution conditions for the flow
          title: Flow stores the info for the flow
      pagination:
        description: Pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: |-
      QueryFlowsForOwnerResponse the response type for the
      Query/FlowsForOwnerRequest RPC
  intento.intent.v1.QueryFlowsResponse:
    type: object
    properties:
      flows:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            owner:
              type: string
            label:
              type: string
            fee_address:
              type: string
            msgs:
              type: array
              items:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }
                      // or ...
                      if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                        foo = any.unpack(Foo.getDefaultInstance());
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := anypb.New(foo)
                       if err != nil {
                         ...
                       }
                       ...
                       foo := &pb.Foo{}
                       if err := any.UnmarshalTo(foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".


                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
            interval:
              type: string
            start_time:
              type: string
              format: date-time
            exec_time:
              type: string
              format: date-time
            end_time:
              type: string
              format: date-time
            update_history:
              type: array
              items:
                type: string
                format: date-time
            self_hosted_ica:
              type: object
              properties:
                port_id:
                  type: string
                connection_id:
                  type: string
              title: base config for ICA
            trustless_agent:
              type: object
              properties:
                agent_address:
                  type: string
                fee_limit:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  title: >-
                    optional, if set to empty array, no fee limit is set and no
                    fees are charged
                connection_id:
                  type: string
                  title: optional for display
              title: config for a trustless agent for flow execution on host chain
            configuration:
              type: object
              properties:
                save_responses:
                  type: boolean
                  title: >-
                    if true, the flow response outputs are saved and can be used
                    in logic
                updating_disabled:
                  type: boolean
                  title: if true, the flow is not updatable
                stop_on_success:
                  type: boolean
                  title: >-
                    If true, will execute until we get a successful Flow, if
                    false/unset will

                    always execute
                stop_on_failure:
                  type: boolean
                  title: >-
                    If true, will execute until successful Flow, if false/unset
                    will always

                    execute
                stop_on_timeout:
                  type: boolean
                  title: If true, will stop if message times oiut
                fallback_to_owner_balance:
                  type: boolean
                  title: >-
                    If true, owner account balance is used when trigger account
                    funds run out
              title: >-
                ExecutionConfiguration provides the execution-related
                configuration of the

                flow
            conditions:
              type: object
              properties:
                feedback_loops:
                  type: array
                  items:
                    type: object
                    properties:
                      flow_id:
                        type: string
                        format: uint64
                        title: flow to get the latest response value from, optional
                      response_index:
                        type: integer
                        format: int64
                        title: index of the responses
                      response_key:
                        type: string
                        title: for example "Amount"
                      msgs_index:
                        type: integer
                        format: int64
                        title: index of the msg to replace
                      msg_key:
                        type: string
                        title: >-
                          key of the message to replace (e.g. Amount[0].Amount,
                          FromAddress)
                      value_type:
                        type: string
                        title: >-
                          can be anything from sdk.Int, sdk.Coin, sdk.Coins,
                          string, []string, []sdk.Int
                      icq_config:
                        description: >-
                          bool calculate_difference = 7; //True: calculate the
                          difference with the previous value instead of using
                          the value directly.


                          config of ICQ to perform
                        type: object
                        properties:
                          connection_id:
                            type: string
                          chain_id:
                            type: string
                          timeout_policy:
                            type: string
                            enum:
                              - REJECT_QUERY_RESPONSE
                              - RETRY_QUERY_REQUEST
                              - EXECUTE_QUERY_CALLBACK
                            default: REJECT_QUERY_RESPONSE
                          timeout_duration:
                            type: string
                          query_type:
                            type: string
                            title: e.g. store/bank/key store/staking/key
                          query_key:
                            type: string
                            title: >-
                              key in the store that stores the query e.g.
                              stakingtypes.GetValidatorKey(validatorAddressBz)
                          response:
                            type: string
                            format: byte
                            title: should be reset after execution
                        title: config for using interchain queries
                    title: >-
                      Replace value with value from message or response from
                      another flow’s

                      latest output before execution
                  title: >-
                    Replace value with value from message or response from
                    another flow’s

                    latest output
                comparisons:
                  type: array
                  items:
                    type: object
                    properties:
                      flow_id:
                        type: string
                        format: uint64
                        title: >-
                          get the latest response value from other flow,
                          optional
                      response_index:
                        type: integer
                        format: int64
                        title: index of the message response, optional
                      response_key:
                        type: string
                        title: e.g. Amount[0].Amount, FromAddress, optional
                      value_type:
                        type: string
                        title: >-
                          can be anything from sdk.Int, sdk.Coin, sdk.Coins,
                          string, []string, []sdk.Int
                      operator:
                        type: string
                        enum:
                          - EQUAL
                          - CONTAINS
                          - NOT_CONTAINS
                          - SMALLER_THAN
                          - LARGER_THAN
                          - GREATER_EQUAL
                          - LESS_EQUAL
                          - STARTS_WITH
                          - ENDS_WITH
                          - NOT_EQUAL
                        default: EQUAL
                        description: >-
                          Comparison operators that can be used for various
                          types.

                           - EQUAL: Equality check (for all types)
                           - CONTAINS: Contains check (for strings, arrays, etc.)
                           - NOT_CONTAINS: Not contains check (for strings, arrays, etc.)
                           - SMALLER_THAN: Less than check (for numeric types)
                           - LARGER_THAN: Greater than check (for numeric types)
                           - GREATER_EQUAL: Greater than or equal to check (for numeric types)
                           - LESS_EQUAL: Less than or equal to check (for numeric types)
                           - STARTS_WITH: Starts with check (for strings)
                           - ENDS_WITH: Ends with check (for strings)
                           - NOT_EQUAL: Not equal check (for all types)
                      operand:
                        type: string
                      icq_config:
                        description: >-
                          bool calculate_difference = 7; //True: Calculate the
                          difference with the previous value.


                          config of ICQ to perform
                        type: object
                        properties:
                          connection_id:
                            type: string
                          chain_id:
                            type: string
                          timeout_policy:
                            type: string
                            enum:
                              - REJECT_QUERY_RESPONSE
                              - RETRY_QUERY_REQUEST
                              - EXECUTE_QUERY_CALLBACK
                            default: REJECT_QUERY_RESPONSE
                          timeout_duration:
                            type: string
                          query_type:
                            type: string
                            title: e.g. store/bank/key store/staking/key
                          query_key:
                            type: string
                            title: >-
                              key in the store that stores the query e.g.
                              stakingtypes.GetValidatorKey(validatorAddressBz)
                          response:
                            type: string
                            format: byte
                            title: should be reset after execution
                        title: config for using interchain queries
                    title: >-
                      Comparison is checked on the response in JSON before
                      execution of

                      flow and outputs true or false
                  title: Comparison with response response value
                stop_on_success_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that when executing
                    succesfully, stops

                    execution
                stop_on_failure_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that when not executing
                    succesfully,

                    stops execution
                skip_on_failure_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that should be executed
                    succesfully

                    after their latest call before execution is allowed
                skip_on_success_of:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    optional array of dependent intents that should fail after
                    their latest

                    call before execution is allowed
                use_and_for_comparisons:
                  type: boolean
                  description: >-
                    True: Use AND for combining comparisons. False: Use OR for
                    combining comparisons.
              title: ExecutionConditions provides execution conditions for the flow
          title: Flow stores the info for the flow
      pagination:
        description: Pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: QueryFlowsResponse the response type for the Query/FlowsRequest RPCπ
  intento.intent.v1.QueryInterchainAccountFromAddressResponse:
    type: object
    properties:
      interchain_account_address:
        type: string
    title: |-
      QueryInterchainAccountFromAddressResponse the response type for the
      Query/InterchainAccountAddress RPC
  intento.intent.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          flow_funds_commission:
            type: string
            format: int64
            title: >-
              Commision rate percentage for returning remaining funds in flow
              fee account after final execution, e.g. 100 = 1X, 250 = 250
          flow_flex_fee_mul:
            type: string
            format: int64
            title: >-
              Multiplier to the flexible flow gas fee in promille, e.g. 1000 =
              1X, 2500 = 2,5x
          burn_fee_per_msg:
            type: string
            format: int64
            title: Fixed burn fee per message execution to burn native denom
          gas_fee_coins:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            title: >-
              Array of denoms that can be used for fee payment together with an
              amount
          max_flow_duration:
            type: string
            title: Maximum period for a flow
          min_flow_duration:
            type: string
            title: Minimum period for a flow
          min_flow_interval:
            type: string
            title: Minimum period for a flow
          connection_rewards:
            type: array
            items:
              type: object
              properties:
                connection_id:
                  type: string
                  title: e.g. "connection-12"
                relayer_rewards:
                  type: array
                  items:
                    type: string
                    format: int64
                  title: 'same format: 0=Low, 1=Medium, 2=High'
            description: >-
              relayer rewards in uinto for each message class 0=Low,1=Medium,
              2=High.

              Rewards are in uinto and topped up in the module account by alloc
              module.
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  intento.intent.v1.QueryTrustlessAgentResponse:
    type: object
    properties:
      trustless_agent:
        title: TrustlessAgent
        type: object
        properties:
          agent_address:
            type: string
          ica_config:
            type: object
            properties:
              port_id:
                type: string
              connection_id:
                type: string
            title: base config for ICA
          fee_config:
            type: object
            properties:
              fee_admin:
                type: string
                title: >-
                  can set fee coins supported, which can be charged per message
                  executed if a fee limit is set in the flow
              fee_coins_supported:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                title: can be empty
    description: >-
      QueryTrustlessAgentResponse is the response type for the
      Query/TrustlessAgent

      RPC method.
  intento.intent.v1.QueryTrustlessAgentsByFeeAdminResponse:
    type: object
    properties:
      trustless_agents:
        type: array
        items:
          type: object
          properties:
            agent_address:
              type: string
            ica_config:
              type: object
              properties:
                port_id:
                  type: string
                connection_id:
                  type: string
              title: base config for ICA
            fee_config:
              type: object
              properties:
                fee_admin:
                  type: string
                  title: >-
                    can set fee coins supported, which can be charged per
                    message executed if a fee limit is set in the flow
                fee_coins_supported:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  title: can be empty
        title: TrustlessAgents
      pagination:
        description: Pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryTrustlessAgentResponse is the response type for the
      Query/TrustlessAgent

      RPC method.
  intento.intent.v1.QueryTrustlessAgentsResponse:
    type: object
    properties:
      trustless_agents:
        type: array
        items:
          type: object
          properties:
            agent_address:
              type: string
            ica_config:
              type: object
              properties:
                port_id:
                  type: string
                connection_id:
                  type: string
              title: base config for ICA
            fee_config:
              type: object
              properties:
                fee_admin:
                  type: string
                  title: >-
                    can set fee coins supported, which can be charged per
                    message executed if a fee limit is set in the flow
                fee_coins_supported:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  title: can be empty
        title: TrustlessAgents
      pagination:
        description: Pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryTrustlessAgentsResponse is the response type for the
      Query/TrustlessAgent

      RPC method.
  intento.intent.v1.TrustlessAgent:
    type: object
    properties:
      agent_address:
        type: string
      ica_config:
        type: object
        properties:
          port_id:
            type: string
          connection_id:
            type: string
        title: base config for ICA
      fee_config:
        type: object
        properties:
          fee_admin:
            type: string
            title: >-
              can set fee coins supported, which can be charged per message
              executed if a fee limit is set in the flow
          fee_coins_supported:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            title: can be empty
  intento.intent.v1.TrustlessAgentConfig:
    type: object
    properties:
      agent_address:
        type: string
      fee_limit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: >-
          optional, if set to empty array, no fee limit is set and no fees are
          charged
      connection_id:
        type: string
        title: optional for display
    title: config for a trustless agent for flow execution on host chain
  intento.intent.v1.TrustlessAgentFeeConfig:
    type: object
    properties:
      fee_admin:
        type: string
        title: >-
          can set fee coins supported, which can be charged per message executed
          if a fee limit is set in the flow
      fee_coins_supported:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: can be empty
  stride.interchainquery.v1.TimeoutPolicy:
    type: string
    enum:
      - REJECT_QUERY_RESPONSE
      - RETRY_QUERY_REQUEST
      - EXECUTE_QUERY_CALLBACK
    default: REJECT_QUERY_RESPONSE
  stride.interchainquery.v1.Query:
    type: object
    properties:
      id:
        type: string
      connection_id:
        type: string
      chain_id:
        type: string
      query_type:
        type: string
      request_data:
        type: string
        format: byte
      callback_module:
        type: string
      callback_id:
        type: string
      callback_data:
        type: string
        format: byte
      timeout_policy:
        type: string
        enum:
          - REJECT_QUERY_RESPONSE
          - RETRY_QUERY_REQUEST
          - EXECUTE_QUERY_CALLBACK
        default: REJECT_QUERY_RESPONSE
      timeout_duration:
        type: string
      timeout_timestamp:
        type: string
        format: uint64
      request_sent:
        type: boolean
      submission_height:
        type: string
        format: uint64
  stride.interchainquery.v1.QueryPendingQueriesResponse:
    type: object
    properties:
      pending_queries:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
            connection_id:
              type: string
            chain_id:
              type: string
            query_type:
              type: string
            request_data:
              type: string
              format: byte
            callback_module:
              type: string
            callback_id:
              type: string
            callback_data:
              type: string
              format: byte
            timeout_policy:
              type: string
              enum:
                - REJECT_QUERY_RESPONSE
                - RETRY_QUERY_REQUEST
                - EXECUTE_QUERY_CALLBACK
              default: REJECT_QUERY_RESPONSE
            timeout_duration:
              type: string
            timeout_timestamp:
              type: string
              format: uint64
            request_sent:
              type: boolean
            submission_height:
              type: string
              format: uint64
  intento.mint.v1.Params:
    type: object
    properties:
      mint_denom:
        type: string
        title: type of coin to mint
      start_time:
        type: string
        format: date-time
        title: the time the chain starts
      initial_annual_provisions:
        type: string
        title: initial annual provisions
      reduction_factor:
        type: string
        title: factor to reduce inflation by each year
      blocks_per_year:
        type: string
        format: uint64
        title: expected blocks per year
    description: Params holds parameters for the mint module.
  intento.mint.v1.QueryAnnualProvisionsResponse:
    type: object
    properties:
      annual_provisions:
        type: string
        format: byte
        description: annual_provisions is the current minting annual provisions value.
    description: |-
      QueryAnnualProvisionsResponse is the response type for the
      Query/AnnualProvisions RPC method.
  intento.mint.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          mint_denom:
            type: string
            title: type of coin to mint
          start_time:
            type: string
            format: date-time
            title: the time the chain starts
          initial_annual_provisions:
            type: string
            title: initial annual provisions
          reduction_factor:
            type: string
            title: factor to reduce inflation by each year
          blocks_per_year:
            type: string
            format: uint64
            title: expected blocks per year
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
